# Agent Harness Core Invariants

Date: 2026-07-19

This catalog is the release-gate source for deterministic simulation, scenario replay, and review evidence. The same invariant IDs are exposed by `agent-harness invariants` through `agent_harness_core::quality::invariant_catalog`.

| ID | Invariant | Owner | Current staging evidence | Remaining live/soak evidence |
|---|---|---|---|---|
| I1 | One allowed inbound message triggers at most one model turn. | `channel_ingress`, `channel_runtime`, `runtime_queue` | Channel receive/enqueue tests; runtime queue idempotency tests. | Burst replay fixture with duplicate inbound events. |
| I2 | Every completed turn has exactly one delivery, explicit error notification, or dead-letter notification. | `runtime_pipeline`, `channel_delivery` | Runtime failure outbox tests; dead-letter timeout policy test. | Live provider timeout/dead-letter user notification sample. |
| I3 | Terminal states are irreversible; pending or retryable worker jobs already at their attempt cap become terminal before selection or lease. | `runtime_pipeline`, `runtime_queue`, `workers` | Timeout receipts are terminal for status/selection; queue retry creates a fresh id instead of resurrecting terminal state; `runtime_worker::tests::queue_skip_receipt_is_sticky_terminal_after_later_non_terminal_receipt`; `runtime_worker::tests::queue_retry_of_terminal_item_creates_fresh_runnable_id_only`; `workers::tests::exhausted_pending_worker_is_terminalized_without_starting_process`. | Operator retry/skip drill against staging queue. |
| I4 | Cancel/stop only affects the requested turn, queue item, worker job, declared scope, or the selected deterministic job's descendant process tree. Windows deterministic-shell timeouts use Job Object containment with `taskkill` fallback. | `admission`, `channel_state`, `runtime_queue`, `workers` | Scoped stop marker/receipt test; existing `/stop` cancel marker tests; `runtime_worker::tests::scoped_stop_marker_blocks_selection_prepare_and_lease`; `workers::tests::deterministic_timeout_terminates_descendant_process_tree`. | Live active-turn and active-job scoped stop receipt. |
| I5 | Crash recovery loses no work and duplicates no side effects. | `queue_shadow`, `supervision`, `runtime_worker` | Lease/idempotency tests; queue shadow divergence tests; supervisor breaker tests. | Seeded crash simulation and seven-day shadow parity summary. |
| I6 | Over-budget work is deferred or blocked, not dropped silently. | `autonomy`, `workers`, `mcp` | SQLite budget counter test; MCP allow-list rejection receipt. | Concurrent budget race fixture under load. |
| I7 | Every accepted ingress has a terminal trace chain reconstructable by one command, and durable provider errors omit request URLs and credentials. | `trace`, `status`, `logging`, `channel adapters` | `trace_harness_event` scans runtime/channel/log receipts and detects terminal status. Codex startup/write failures record terminal protocol-error receipts with `codex_runtime::tests::run_codex_runtime_records_terminal_receipt_when_app_server_exits_during_startup`. `agent-harness-cli::tests::provider_error_url_redaction_removes_transport_credentials` proves provider transport diagnostics retain no absolute provider URL, token, userinfo, or host. | Live traceId sample across ingress, runtime, outbox, and delivery, plus a post-cutover log scan showing no provider request URLs or credentials. |
| I8 | Agent identity and `/new` task boundaries are routing boundaries across channel state, session freshness, prompt, skill source/eligibility/usage priors, runtime, outbox, delivery, and memory namespaces. | `channel_state`, `runtime_pipeline`, `prompt`, `turns`, `skills`, `skill_usage`, `memory` | `turns::tests::multi_agent_skill_matrix_isolates_workspaces_allowlists_and_usage_priors`; `skill_usage::tests::skill_usage_snapshot_for_agent_excludes_other_agent_events`; non-main prompt isolation tests; `runtime_pipeline::tests::channel_session_freshness_does_not_cross_suppress_other_agent`; `prompt::tests::prompt_bundle_new_command_boundary_skips_prior_task_memory_context`; `memory::tests::non_main_memory_prompt_context_excludes_global_imported_snapshot_by_default`; `memory::tests::public_agent_read_path_smoke_surfaces_source_allow_list_and_filtered_counts` for public/non-main memory source allow/deny and filtered-count telemetry. | Candidate-home main/non-main/third-agent skill-selection replay plus live non-main same-DM replay with final outbox and delivery receipt; candidate-home smoke showing `/new` fresh prompt/memory task-boundary telemetry and public/non-main read-path scope/trust telemetry. |
| I9 | Final channel replies exclude progress/narration stream content and review-only evidence when the parent workflow has not completed. | `runtime_pipeline`, `progress`, `channel_delivery`, `codex_runtime` | Defined by `docs/agent-harness-assistant-narration-tech-note.md`; Round10 adds `runtime_pipeline::tests::already_recorded_completion_repair_keeps_progress_panel_out_of_final_outbox` and `runtime_pipeline::tests::already_recorded_completion_repair_keeps_progress_panel_out_of_discord_final_outbox` for the completion-repair path. Follow-up coverage adds `codex_runtime::tests::run_codex_runtime_rejects_stdout_recovery_narration_without_final_answer`, proving stdout recovery fails closed when a completed turn has progress-panel narration but no `final_answer`. Round12 scenario-matrix follow-up adds `progress::tests::progress_surface_volume_replay_converges_without_post_terminal_churn`, proving Telegram and Discord progress panels cap body/action edits, keep status/current-step alive, converge terminally, and suppress late post-terminal churn while final-surface separation remains covered by the runtime-pipeline outbox tests. Package D adds `progress::tests::action_stream_summarizes_internal_worker_results_instead_of_raw_final_text` and `progress::tests::action_stream_summarizes_structured_subagent_notifications_without_source_label`, proving worker/subagent result payloads, including structured subagent notifications without source labels, are summarized in action progress instead of leaking raw worker final text. Round14/Round17 final-outbox coverage adds `runtime_pipeline::tests::run_runtime_queue_once_suppresses_read_only_review_final_for_implementation_goal`, proving implementation-goal read-only review/evidence output is not written as `agent-reply` before parent workflow completion; `runtime_pipeline::tests::run_runtime_queue_once_suppresses_owner_mismatched_agent_final_outbox`, proving completed output whose run agent/session does not match the lane-owning agent/session stays internal evidence; `runtime_pipeline::tests::run_runtime_queue_once_writes_final_outbox_for_non_main_agent_owned_group_lane`, proving a channel-ingress-routed non-main agent turn on its own lane is a first-class final owner; `channel_delivery::tests::outbox_plan_treats_permanent_skip_as_terminal_not_delivered`, proving already-written invalid final outbox rows can be retired with `skipped-permanent` receipts without counting as delivered; and `progress::tests::terminal_progress_waits_until_source_final_delivery_is_delivered`, proving terminal progress waits for the same source queue's final provider delivery when a final outbox row exists. | Full provider replay scenario with long progress narration, compact recovery fallback or fresh-thread recovery, short final answer, worker-result progress events, review/evidence-only output, skipped-permanent cleanup readback, and Telegram/Discord outbox/delivery assertions. |
| I10 | Active Codex tool-use idle timeouts are stopped and routed through bounded recovery, virtual-session continuation, or an explicit terminal trace instead of directly dead-lettering the parent task. | `codex_runtime`, `runtime_pipeline`, `trace`, `context_rollover`, `prompt` | Round10 adds `codex_runtime::tests::run_codex_runtime_recovers_tool_use_idle_timeout_with_fresh_thread`, proving a stalled `commandExecution` item is stopped, recorded as `toolUseTimeout`, and continued through one fresh-thread recovery. Round17 interrupted-task staging adds `runtime_pipeline::tests::tool_timeout_fallback_failure_retry_requeues_continuation`, `runtime_pipeline::tests::plain_provider_timeout_does_not_requeue_interrupted_continuation`, and `codex_runtime::tests::retry_after_timeout_rotates_stale_stdout_before_recovery`, proving failed tool-timeout fallback can roll to a virtual-session continuation, provider outage-shaped timeouts stay terminal/retry-shaped, and stale timeout stdout is rotated before retry recovery can impersonate completion. | Broader per-tool cancellation/kill semantics, resumable tool supervisor support, and live long-review soak evidence. |
| I11 | Binary and bulky artifacts enter durable main-session context only as harness artifact references plus bounded extraction summaries; raw bytes, base64, provider URLs, and large tool blobs stay in artifact storage or receipts, including expanded inbound media kinds and referenced-message media. | `media`, `prompt`, `runtime_worker`, `codex_runtime`, `workers`, `memory`, `discord ingress`, `telegram ingress` | Round10 adds `media::tests::artifact_prompt_hygiene_renders_generic_bounded_summaries_not_payloads` and extends `runtime_worker::tests::prepare_runtime_queue_item_round_trips_inbound_media_artifacts_to_prompt`, proving image, audio/transcript, generated speech/media, browser capture, document/download, long tool-log/review transcript, worker-report, and provider-native media artifacts render as refs plus summaries without base64/provider payload leakage. Follow-up staging adds `codex_runtime::tests::codex_progress_redacts_artifact_payloads_from_tool_previews`, proving generated media/tool-output progress previews are redacted before durable progress events can persist raw `data:image`, base64, provider URLs, or token-bearing strings. Discord attachment extraction follow-up adds Discord text/image artifact tests proving provider URL/token redaction, host-gated downloads, fake-image rejection, and prompt-safe `.json` paths. Channel media staging adds Telegram document/voice/audio/static-sticker/reply-media tests and Discord referenced-media tests, proving expanded media kinds and replied-to media remain bounded artifacts with `current`/`referenced` provenance rather than prompt payloads. | End-to-end artifact-heavy Telegram/Discord replay proving extraction writes bounded facts, raw artifacts remain retrievable by pointer, compact/fresh-thread recovery avoids reusing polluted context, and memory/continuity do not persist raw artifact blobs. |
| I12 | Active `mem-engine` ownership uses the openclaw-mem bridge as the primary write/recall path when configured; recall fallback remains read-only and store fails closed unless the memory layer accepts the write. | `memory`, `memory_owner`, `openclaw-mem bridge` | Local staging adds `memory::tests::mem_engine_owner_recall_invokes_configured_bridge_command`, `memory::tests::mem_engine_owner_store_invokes_configured_bridge_command`, `memory::tests::mem_engine_owner_recall_uses_bridge_command_from_harness_config`, and `memory::tests::mem_engine_owner_status_uses_configured_bridge_status_from_harness_config`, proving configured bridge subprocess transport, harness-config-backed routing for supervisor restarts, request/response mirrors, status telemetry that is not stale fallback, openclaw-mem policy source, accepted recall payloads, and approved-store forwarding. Existing fallback tests still prove unreachable bridge recall degrades to `migration-fallback` without writes. | Operator cutover must configure the bridge command or binary through env or the live harness config, run `memory-service-status` and `memory-read-path-smoke`, prove `fallbackUsed=false` for a known query, and prove approved store returns a bridge `storeId` without using harness-local writeback fallback. |
| I13 | Inline-image, native-image-input, oversized-output polluted Codex threads, interrupted long-task terminal failures, and repeated high-usage stream-unstable retries feed bounded context-rollover continuity before parent error delivery when continuation gates allow it. | `codex_runtime`, `context_rollover`, `runtime_queue`, `prompt`, `runtime_pipeline` | Existing compact, stream-unstable, and polluted-thread tests prove official compact accounting, idempotent compact counters, inline-image polluted-thread fresh-thread rollover, terminal polluted-thread continuation guards, sibling-safe requeue, and repeated high-usage stream retry behavior. Channel media staging adds `codex_runtime::tests::context_preflight_rolls_over_bound_thread_native_image_bloat`, proving pending native image input bytes are counted by the same preflight health gate before native image input is widened. Cutover B adds resolver-backed bounded prompt context, `maxContinuationDepth` separated from compact-count thresholds, stream-unstable continuation knobs, `/new` virtual-session closure, Codex `codexThreadId` backfill, and `terminal-failed` max-depth virtual-session marking, with S1-S5 `virtual-session-rollover` scenario-matrix coverage. Round17 interrupted-task staging adds `runtime_pipeline::tests::no_final_answer_terminal_interruption_requeues_continuation` and prompt coverage for `lastInterruption` continuation guidance. | Live soak/readback must prove model-visible continuity through real Telegram/Discord turns, bounded working-set prompt injection, final outbox trace continuity, and `/new` task-boundary behavior without synthetic mutation of an active lane. |
| I14 | Rich outbound presentation is rendered by provider adapters from a trusted semantic payload only when automatic projection proves complete canonical text coverage; otherwise delivery uses provider-safe canonical plain-text chunks before any rich unit. `fullTextPreserved=true` requires every required ordered text unit to be accepted, while model-authored raw Telegram/Discord syntax must not be the safety boundary and media units retain attachment-kind accounting. | `runtime_pipeline`, `channel_delivery`, `progress`, `media`, `trace` | Existing schema, old-outbox, escaping, media, fallback, and rendered-unit tests remain. Issue #9 coverage adds the 16/17-block loss-aware bridge boundary, persisted legacy lossy-row assessment, Discord <=2,000-character and Telegram UTF-16 canonical fallback seams, failed-chunk preservation truth, explicit rendered-receipt proof, and `rich-final-fidelity-and-fallback` durable outbox re-open/idempotency replay. | Live Telegram/Discord ordinary-chat smoke showing provider-rendered ordinary finals and a controlled long-final suffix, media-unit attachment-kind receipt samples, artifact/provider URL redaction tests for generated presentation payloads, and clicked-action interaction re-entry through channel identity plus `agentId`/session permission gates. |
| I15 | Concrete channel session history is lane-bound: session/private recall candidates require the same agent and lane-qualified session key, while broad project/global recall must be explicit. | `memory_pack`, `memory`, `prompt`, `context_rollover` | Channel session history classification staging adds `memory_pack::tests::retrieval_scope_session_requires_same_agent_and_session_key`, `memory_pack::tests::retrieval_scope_session_rejects_bare_session_key_even_when_equal`, `memory_pack::tests::retrieval_scope_agent_private_rejects_bare_session_key_even_when_equal`, `memory_pack::tests::retrieval_candidate_does_not_fall_back_to_wrong_lane_concrete_history`, `memory_pack::tests::retrieval_candidate_uses_project_after_wrong_lane_concrete_candidate`, `memory_pack::tests::retrieve_wrong_lane_concrete_history_reports_scope_denied_not_missing`, `memory_pack::tests::retrieve_wrong_lane_concrete_then_project_returns_explicit_broad_scope`, and `memory_pack::tests::retrieve_wrong_lane_concrete_then_global_imported_returns_explicit_broad_scope`, proving concrete scope is same-agent/session only, bare/malformed concrete session keys are denied even when equal, wrong-lane concrete candidates do not become arbitrary fallbacks, denial receipts are `scope-denied` rather than missing, and explicit `project`/`global-imported` scopes remain retrievable after denied concrete candidates. Cutover B virtual-session resolver and prompt tests extend the same rule to model-visible continuity: deterministic resolver state must use exact `platform/accountId/channelId/userId/agentId/sessionKey` evidence and must not substitute another lane when reply metadata is missing. | Live or candidate-home cross-session search smoke should query a prior-session scenario with Telegram/Discord and main/non-main axes, prove receipts expose same-lane vs denied reasons, and prove `project`/`global-imported` fallback is intentionally broad and bounded by the harness-home store partition. Live virtual-session soak should also confirm same-lane resolver anchors appear in prompt readback without cross-platform or cross-agent substitution. |
| I16 | Outbound channel attachments originate only from policy-validated local paths or resolvable harness artifacts; directive-like text inside protected spans is never delivered; rejected directives leave a visible note plus a machine-readable receipt. | `runtime_pipeline`, `media_delivery_policy`, `channel_delivery` | Channel media staging adds parser tests for protected spans and unknown extensions, policy tests for accepted workspace files and denied paths, artifactRef resolution/policy rejection tests, media lint warning/fail-closed tests, prompt contract tests, Telegram album/caption tests, Discord multipart tests, and outbound media policy/lint receipts. | Live Telegram/Discord media smoke should include a valid generated attachment, a rejected denied-prefix path, and a rich artifactRef media delivery trace, with policy receipts and visible degradation verified before widening native image input or lint fail-closed config. |
| I17 | Durable control artifacts are authoritative before runtime execution, progress delivery, restart consumption, and sender-class cron notification. Queue-skip, scoped-stop, quarantine, restart requests, cron freshness expectations, and sender source receipts must be checked before acting; consumption/suppression must be receipted with enough identity to audit the decision. | `runtime_worker`, `runtime_pipeline`, `runtime_queue`, `progress`, `channel_runtime`, `status`, `health`, `cron_scheduler`, `dream_director` | `runtime_worker::tests::queue_skip_receipt_is_sticky_terminal_after_later_non_terminal_receipt`, `runtime_worker::tests::scoped_stop_marker_blocks_selection_prepare_and_lease`, `runtime_worker::tests::suppressed_receipt_emitted_at_most_once`, `runtime_worker::tests::lease_reconcile_respects_terminal_controls`, `runtime_worker::tests::queue_retry_of_terminal_item_creates_fresh_runnable_id_only`, `runtime_pipeline::tests::prepared_protocol_error_terminalizes_after_threshold`, `runtime_pipeline::tests::scoped_stop_suppresses_missing_prepared_execution_before_no_prepared_churn`, `runtime_pipeline::tests::quarantine_suppression_records_terminal_progress_once_then_silent`, `runtime_pipeline::tests::untargeted_terminal_control_suppression_appends_progress_with_queue_id`, `progress::tests::terminal_control_marker_suppresses_cached_nonterminal_ghost_queue`, `runtime_pipeline::tests::e2e_1_ghost_queue_replay_from_sanitized_fixture`, `agent-harness-cli::tests::e2e_4_restart_gateway_staging_closed_loop_replay`, `health::tests::healthz_warns_on_stale_keeper_receipt`, `health::tests::healthz_evaluates_all_canon_monitor_blocks_generically`, `cron_scheduler::tests::restart_catch_up_respects_per_job_policy`, `cron_scheduler::tests::e2e_5_cron_outage_replay_from_sanitized_fixture`, `dream_director::tests::dream_director_sender_suppresses_stale_source`, and `dream_director::tests::dream_director_sender_accepts_absolute_source_path_from_relative_home` prove queue controls, progress terminalization, terminal-control progress close/silence, restart/catch-up decisions, cron-canon freshness warnings, stale sender suppression, relative-home freshness path normalization, and the checked-in E2E-1/E2E-4/E2E-5 replay surfaces. | Live or candidate-home replay of a terminal-controlled pending queue must show no resurrected execution, exactly one `suppressed` receipt with `terminalControlMatched`, one final terminal progress surface with `progressSuppressedReason=terminal-control-present`, no further status edits, and no continuing `no-prepared-execution` churn. Restart promotion still needs real supervised child restart plus provider-side Discord delivery evidence beyond the CLI staging replay. Sender-class jobs still need live/candidate freshness receipts, and any current stale cron source state needs an explicit operator decision before promotion. |
| I18 | Terminal channel delivery receipts outrank later retryable receipts for the same delivery id. A delivery with any terminal `delivered` or `skipped-permanent` receipt must not return to pending because an in-flight sender appends a later retryable `failed` audit receipt. | `channel_delivery`, `channel_runtime` | `channel_delivery::tests::terminal_delivery_receipt_outranks_later_retryable_failed_receipt` proves `skipped-permanent` followed by later `failed` stays terminal and logs the superseded-by-terminal audit condition. Telegram sender tests prove overlong finals are chunked before provider send, post-chunk `message is too long` is classified permanent, and permanent send errors record terminal skip instead of retryable failure. | Live Telegram/Discord outbox plans after cutover must show no resurrected pending row for delivery ids with terminal receipts; any manual `skipped-permanent` cleanup must remain terminal even if a prior sender attempt finishes late. |
| I19 | Interruption is not test failure. A same-lane newer turn that cancels an in-flight Codex command must record `interrupted_by_new_turn` with bounded command evidence and resume guidance, not summarize the interrupted command as a failed test unless a failing exit code or assertion output was observed. | `codex_runtime`, `runtime_pipeline`, `virtual_session_context`, `prompt` | `codex_runtime::tests::interrupt_running_shell_command_records_interrupted_status` records active `commandExecution` evidence, cwd, logs, and safe-rerun classification on cancel; `runtime_pipeline::tests::interrupted_verification_not_counted_as_test_failure` prevents failure wording without an exit code; `prompt::tests::prompt_bundle_includes_virtual_session_resolver_context_section` proves structured interruption receipts feed bounded `lastInterruption` continuation context while legacy prose remains fallback. | Live or controlled-lane smoke may interrupt only the current execution lane and must leave gateway/supervisor loops alive; post-cutover receipts should show `interruptionReason=interrupted_by_new_turn`, bounded command preview, and no "failed test" claim unless real failure output exists. |
| I20 | Skill ecosystem mutation is proposal-only by default. Any apply requires an explicit CLI or worker/config authorization and must pass lint, guard, checksum, backup, and receipt gates; archives are reversible moves rather than deletion. Retired skills never reach prompt selection; `agents`, `disable-model-invocation`, and `user-invocable` independently gate agent eligibility, automatic selection, and explicit selection. | `skills`, `skill_usage`, `turns`, `skill_apply`, `skill_guard`, `skill_lint`, `skill_curator`, `prompt`, `workers` | `skills::tests::skill_selection_agent_allowlist_is_fail_closed_for_model_and_explicit_invocation`; `turns::tests::multi_agent_skill_matrix_isolates_workspaces_allowlists_and_usage_priors`; `skill_usage::tests::skill_usage_snapshot_for_agent_excludes_other_agent_events`; `workers::tests::skill_synthesis_worker_defaults_to_proposal_only_without_apply_authority`; plus focused matcher, view, lint, guard, curator, pack, apply, synthesis, and doctor tests cover lifecycle/invocation gates, traversal-guarded reads, reversible archive, proposal-only default, and explicitly authorized apply. | Before live cutover, run the closed-loop scenario from a staging home: CJK selection, retired/disabled/agent frontmatter gates, multi-agent source/usage isolation, no-skill complex turn, proposal-only synthesis with zero skill-file mutation, an independently authorized apply replay, re-selection, pack round-trip, curator dry-run, `skill-doctor --json`, and public/private hygiene. |
| I21 | reasoning effort is authorized against the effective provider/model route and preserved exactly through queue and runtime; /think and /reasoning are aliases of one last-write-wins state, GPT-5.6 max remains distinct, ultra is rejected as a reasoning effort, and legacy aliases canonicalize before duplicate handling | `channel_commands/channel_state/channel_runtime/model_catalog/runtime_queue/codex_runtime` | Scenario `gpt56-reasoning-capability`: `codex_capability::tests::paginated_catalog_filters_reserved_ultra_and_keeps_future_efforts_distinct`; `codex_capability::tests::collector_deduplicates_legacy_aliases_after_effort_normalization`; `channel_runtime::tests::model_catalog_authoritative_reasoning_preserves_exact_sol_max`; `channel_runtime::tests::think_and_reasoning_alias_share_one_last_write_wins_state`; `channel_runtime::tests::exact_ultra_is_rejected_as_non_effort_for_both_command_aliases`; `runtime_queue::tests::enqueue_channel_agent_turn_serializes_exact_max_reasoning_policy`; `codex_runtime::backend_reasoning_wire_tests::turn_start_effort_is_exact_and_runtime_kill_switch_is_fail_closed`. | Verify the exact GPT-5.6 route, preserve `max` command-to-wire, keep both command aliases identical, and prove cached plus app-server capability observations filter exact `ultra`. `max` is the highest currently known legal GPT-5.6 effort; future names remain open-ended only when advertised by the exact route. Exact `ultra` is neither advertised nor configurable, while `ultra-high` / `ultra_high` canonicalize to one `xhigh` entry. |
| I22 | each agent prompt is assembled from its own canonical manifest under an exact full-lane and backend-generation key; aliases are fallback-only, deletions create tombstones, and another agent or lane cannot reuse the ledger entry | `prompt/turns/virtual_session_context/operation_plan/runtime_worker` | Scenario `per-agent-prompt-manifest`: `turns::tests::prompt_file_aliases_are_fallback_only_and_conflicts_are_deterministic`; `prompt::tests::prompt_manifest_tracks_generation_reinjection_and_delete_tombstone`; `prompt::tests::prompt_ledger_exact_lane_digest_separates_account_runtime_and_root_axes`; `prompt::tests::operation_plan_prompt_exact_lane_requires_matching_digest_without_legacy_fallback`; `operation_plan::tests::operation_plan_v2_persists_exact_lane_digest_and_rejects_mismatched_duplicate`; `virtual_session_context::tests::exact_lane_denies_legacy_unknown_axes_instead_of_wildcard_matching`; `prompt::tests::prompt_bundle_new_command_boundary_skips_prior_task_memory_context`. | Verify two agents sharing platform/channel/user coordinates, deterministic add/change/delete reinjection, and all eight static canonical files: `AGENTS.md`, `SOUL.md`, `TOOLS.md`, `USER.md`, `IDENTITY.md`, `HEARTBEAT.md`, `BOOTSTRAP.md`, and agent-scoped `MEMORY.md`. `AGENT.md` and `BOOT.md` are fallback aliases; dynamic recall remains separate. `/new` and backend-generation changes must not reuse stale static or OperationPlan context. |
| I23 | each child has an immutable independent model/effort policy and exact master-owned result owner; trusted runtime routing is derived from `WorkerJobKind`; child results enter the durable mailbox and never directly create parent progress, final, or error output | `child_execution_policy/worker_adapters/workers/worker_result_mailbox/runtime_pipeline` | Scenario `master-owned-subagent-coordination`: `child_execution_policy::tests::heterogeneous_siblings_preserve_independent_open_ended_routes_and_efforts`; `worker_adapters::tests::subagent_adapter_v5_atomically_preserves_heterogeneous_models_and_efforts`; `worker_adapters::tests::subagent_adapter_v5_rolls_back_children_wait_and_watchdog_together`; `worker_adapters::tests::controlled_coordinator_smoke_enqueues_only_two_catalog_admitted_children`; `worker_adapters::tests::controlled_coordinator_smoke_rejects_stale_live_session_before_persisting_workers`; `worker_adapters::tests::controlled_coordinator_smoke_rejects_non_authoritative_child_agent_before_persisting_workers`; `worker_adapters::tests::controlled_coordinator_smoke_rejects_ultra_before_persisting_workers`; `worker_result_mailbox::tests::unread_lookup_and_claim_are_isolated_by_every_exact_owner_axis`; `workers::tests::child_policy_preserves_heterogeneous_sibling_routes_in_runtime_queue`; `worker_runtime_terminal::llm_runtime_queue_is_not_terminal_until_correlated_runtime_receipt`; `runtime_pipeline::tests::failed_worker_child_with_channel_context_does_not_write_parent_error_outbox`. | Carry heterogeneous siblings through terminal insertion and verify the actual outcome is reduced to a redacted summary of at most 4 KiB plus at most 16 opaque pointers. Queue id, typed receipt schema, and `WorkerJobKind`-derived runtime-class/origin provenance must match; prompt context contains only validated envelopes and structural child metadata. A controlled live replay must require explicit marker-protected admission for the current exact main-lane session and every planned child execution agent before it can use the normal V5 path. Exact ownership is preserved, missing/invalid expected evidence becomes an explicit failed omission, child output cannot directly deliver progress/final/error surfaces, and only the master/coordinator synthesizes user-facing output. |
| I24 | coordinator resume is durable, exact-lane, coalesced, and at-most-once: any active lease in the exact master lane suppresses wakeup, a confirmed released lane schedules one typed continuation, and mailbox acknowledgement follows continuation lease acquisition | `worker_coordination/worker_resume/coordinator_resume/workers/runtime_worker` | Scenario `master-owned-subagent-coordination`: `worker_resume::tests::active_parent_quarantines_without_claiming_then_idle_schedules_once`; `worker_resume::tests::restart_replays_same_claim_and_intent_without_duplication`; `coordinator_resume::released_parent_schedules_one_durable_coordinator_resume_and_no_master_wakeup`. | Cover the original parent lease, newer same-lane activity, released-lane scheduling, duplicate watchdog passes, process restart, and claim recovery. Exactly one logical durable resume must result: the enqueue contract is at-most-once, deterministic intent reuse prevents duplication, durable reclaim prevents loss, and mailbox acknowledgement occurs only after continuation lease acquisition. |
| I25 | Interactive ingress, progress, runtime completion, and final delivery use bounded committed receipt snapshots. They signal, but never synchronously run, expensive retention; source-aware retention runs only under the isolated `ledger-maintenance` owner. | `runtime_receipt_history/channel_delivery_history/progress_history/ledger_maintenance/supervisor` | `ledger_maintenance` wake/no-op/failure-isolation tests; runtime and delivery busy-lock snapshot tests; 100k channel-delivery and progress-history compaction tests. | Controlled long-turn evidence must show first progress/final delivery does not wait for receipt compaction, while the dedicated maintenance owner remains healthy and bounded. |
| I26 | A Windows Task Scheduler plan preserves every enabled configured `supervisor.telegramLoops` runner while adding `ledger-maintenance-loop`; plan and reconcile derive custom channel-loop identity from the same harness config. | `supervisor/harness_config/supervisor_inventory` | Scenario `supervisor-plan-configured-loop-parity`: `supervisor::tests::plan_includes_enabled_configured_telegram_loop_with_ledger_maintenance`; ledger-owner inventory/reconcile regressions. | A staged plan must include the main Telegram loop, every enabled configured Telegram loop, and `ledger-maintenance-loop`; post-cutover readback must show all expected owner heartbeats fresh. |
| I27 | Provider-visible progress delivery is source-authoritative and stop-responsive: a non-fresh source snapshot cannot replay cached state, historical events without a known provider surface cannot create a fresh surface, and a stop request releases unattempted fresh-send claims before provider I/O. | `progress/progress_event_index/agent-harness-cli/supervisor` | `progress::tests::progress_delivery_defers_cached_events_while_index_snapshot_is_not_fresh`; `progress::tests::progress_delivery_does_not_fresh_send_historical_event_without_existing_surface`; `agent-harness-cli::tests::progress_delivery_stop_releases_unattempted_surface_claims_before_provider_io`. | Candidate and live replay must show no stale cached progress replay under source-index contention, no late provider surface for historical queues, and a watched owner stop that exits without sending or stranding claims. |
| I28 | Skill routing shadow is exact-lane and observability-only; the independently activated manifest observer, task intent, frozen skill revisions, first-load versus rehydration accounting, outcomes, classification, attribution, and terminal learning review are owned by one exact-lane `virtualSessionId`. F1 alone creates no manifest/delivery evidence, F2 cannot change active serving, concrete-session rollover cannot rematch or double-count use, and `/new` closes the manifest and inherits no prior task skill state. | `skill_shadow_runtime/runtime_worker/virtual_skill_manifest/context_rollover/virtual_session_context/prompt/skill_usage/skill_episode/knowledge_classifier/skill_improvement/self_improvement` | Scenario `skill-ecosystem-virtual-evidence`: F1/F2 byte-identity tests; exact virtual digest and runtime-pipeline tests; `virtual_skill_manifest::tests::t3_skill_virtual_evidence_replay_is_exact_idempotent_and_proposal_only` covers two rollovers, retry dedupe, wrong-lane rejection, unknown-until-verified episode/review joins, unverified proposal refusal, and `/new`; focused classifier/proposal tests cover deterministic classification and reversible proposal-only output. | Before C1, prove F1 shadow off/on prompt bytes and active selections are identical and manifest/delivery/usage/learning/proposal files remain untouched. Before live activation, rerun the exact-lane T3 with the stopped-state candidate, feature-disable rollback, and privacy scan. Serving and catalog activation remain separate gates. |
| I29 | Agent-library dream and topology maintenance is proposal-first, non-indexable, idempotent, and subordinate to foreground work: one agent/library writer lease owns staged consolidation, generated reports cannot re-enter matching or learning, unchanged inputs skip semantic review, and activation occurs only at a later catalog epoch with an independently checksummed rollback bundle. | `skill_dream_jobs/skill_dream_workspace/skill_dream/knowledge_classifier/skill_topology/skill_replay/cron_scheduler` | Focused lease/heartbeat/lock-loss, trigger-equivalence, no-change, invalid-artifact, source-admission, report-isolation, topology downgrade, and snapshot/restore tests. | Before unattended enablement, pass T3 replay and a controlled T4 scheduled shadow soak; verify zero serving mutation, zero report re-ingestion, foreground-yield behavior, exact-agent provenance retention, and successful catalog/topology downgrade. |
| I30 | Every candidate and live Codex backend starts from one deployment-owned canonical executable. Exact version, executable SHA-256, npm/native package provenance, provider-scoped `CODEX_HOME` identity, config digest, and parent/child lifecycle are fail-closed and receipted; PATH and global npm fallback are forbidden. | `codex_backend_provenance/supervisor/codex_runtime` | `codex_backend_provenance::tests::exact_version_parser_rejects_drift_and_suffixes`; `codex_backend_provenance::tests::owned_path_gate_rejects_global_or_sibling_executable`; supervisor plan tests for canonical discovery and missing-executable failure; a candidate probe against the exact pinned bundle. | Before candidate promotion, produce install/candidate/startup receipts that agree on canonical path, version `0.144.5`, executable SHA-256, npm integrity, config/CODEX_HOME identity, and lifecycle. Any mismatch or fallback blocks startup/cutover. |
| I31 | The exact Codex 0.144.5 JSON-RPC surface is fixture-locked for initialization, thread start/resume, turn lifecycle, token/context accounting, goal state, compact acknowledgement/items, account lifecycle, capability/config reads, web-search items, and error/null behavior. An upstream completion without exact operation correlation cannot close a harness compact or goal transition. | `codex_protocol_compat/codex_runtime/context_rollover/coordinator_resume/goal` | `codex_protocol_compat::tests::exact_0_144_5_protocol_fixture_covers_cross_cutting_contracts`; schema-bundle SHA-256 evidence; existing compact/reasoning handshake regressions. | Run the sanitized T2 fixture plus compact/resume fail-first replay. A package upgrade or app-server method/field drift requires refreshed schema evidence; `interrupted:null` must remain unattributed until the harness matches the exact compact operation. |
| I32 | Backend authentication is isolated by a current-operator-only harness-owned provider-scoped `CODEX_HOME` and controlled only through an interactive operator lifecycle. Durable state contains readiness generation and one-way exact operation correlation, but never credentials, login challenges, account identities, or full auth URLs. Runtime-ready requires same-generation `account/read` and `model/list` evidence bound to canonical executable provenance; an unready normal turn becomes nonterminal `auth-deferred`, produces no final/error outbox, consumes no retry budget, and is woken exactly once only after a newer ready generation. | `backend_auth/codex_runtime/agent-harness-cli/runtime_worker/security` | `backend_auth::tests` covers Unix/Windows restrictive-home enforcement and verification, empty-home isolation, structured diagnostic rejection, secret-free state, one-writer lease, exact completion correlation, browser/device/API-key-stdin/logout contracts, operator cancel/child kill, orphaned-login restart reconciliation, expiry, account+capability readiness, doctor consistency, generation fences, append-before-mark crash recovery, and exactly-once wake. `runtime_pipeline::tests::auth_deferred_turn_has_zero_delivery_side_effects_and_wakes_once_when_ready` replays ingress through preflight, durable defer, zero delivery, queue blocking, and one wake; exact 0.144.5 empty-home staging probe returns `unauthenticated`. | Add approved real interactive auth evidence, full integrated active-goal/stop/newer-steer T3 campaign replay, existing-live-home ACL migration/backup evidence, crash/support-bundle scan, and operator-approved T4 auth refresh/restart evidence before live activation. |
| I33 | Backend-reported model context capacity is durable policy authority only for the same Codex binding, provider/model route, and backend context generation while fresh. Pollution and model capability policy run first, fresh live ratio then static route ratio run next, and the absolute 120000 guard is an unknown/stale-capacity failsafe only. | `codex_runtime/virtual_session_runtime_index/context_rollover` | Fail-first parser replay proved `modelContextWindow=258400` was dropped. `codex_runtime::tests::token_usage_event_updates_usage_without_turn_completed`, `virtual_session_runtime_index::tests::codex_usage_prefers_normalized_binding_then_global_fallback_and_tails`, `codex_runtime::tests::preflight_uses_fresh_binding_route_generation_capacity_before_120k_failsafe`, and `codex_runtime::tests::preflight_rejects_wrong_backend_generation_capacity_and_keeps_unknown_failsafe` prove field retention, restart/index retention, cross-binding capacity stripping, the 134993/258400 and 204759/258400 boundary decisions, and generation rejection. | Add the exact compact/resume/active-goal T3 incident replay, compact lifecycle correlation, safe rollover, and candidate/live readback before promotion. |
| I34 | A resumed Codex thread must pass a bounded settle before same-thread work. A restored goal turn is drained and isolated from queued-turn output. Compact success requires exact RPC/thread/compact-item evidence; unrelated turn completion is observational, `error:null` is absent, and death after acknowledgement is ambiguous. Safe fallback checkpoints state, projects the active source-thread goal, opens a replacement generation, and releases work only after an acknowledged `goal/set` plus matching active-goal echo and checkpoint/projection checksums. | `codex_runtime/context_rollover/goal` | Resume/first-incident/reordered/null schedules are covered by the CMP-01/CMP-02 tests. `compact_attempt_death_after_ack_is_ambiguous_not_success` now also proves the ambiguous attempt stays non-success while checkpoint/new-thread fallback projects the sanitized active goal, records completion-criteria/projection/checkpoint SHA-256 values, receives a matching active echo on `thread-fresh`, and only then completes the fallback turn. | Add authoritative exact-lane v2 state, unified goal transition/exactly-once continuation/final gating, duplicate/restart T3, and candidate/live readback. |
| I35 | New production completed-turn state writes only exact-account v2 working-set/index/virtual-session artifacts and emits a full eight-axis lane digest plus backend-generation authority receipt. Legacy accountless v1 state is readable only through one-way default-account migration with exact non-account axes; non-default or unknown axes never wildcard-match. | `runtime_pipeline/context_rollover/virtual_session_context/lane` | `runtime_pipeline::tests::run_runtime_queue_once_records_agent_reply_outbox` proves the production completion creates v2 state and `virtual-session-authority.v1`, creates no legacy session index, resolves via a canonical full lane, and retains an opaque prompt reference. `exact_v2_lane_isolates_accounts_agents_and_redacts_prompt_working_set_path`, `non_default_full_lane_does_not_read_legacy_working_set`, and `default_full_lane_migrates_only_an_exact_legacy_record` cover isolation and migration. | Bind goal lineage/continuation intents and skill manifests to the same v2 authority receipt, add stale-generation/restart T3, and verify no legacy new writes in the candidate-home replay. |
| I36 | Active backend goal rows are projected through exact-v2 lane, virtual-session, backend-generation, source thread/turn, goal reference, checksum, and observation-order identity. A campaign has at most one runnable lineage; duplicate, stale, or orphan rows remain auditable and become non-runnable only through validated append-only supersession, never deletion or wrong-lane substitution. | `goal_lineage/codex_runtime/context_rollover/runtime_pipeline` | `goal_projection_observer_captures_normal_and_status_only_updates_fail_closed` covers all-path observer state, stable goal/turn references, status-only inheritance, and incomplete fail-closed evidence; `latest_completed_goal_projection_cannot_rehydrate_an_older_active_goal` prevents terminal-to-active resurrection. `goal_lineage::tests::doctor_is_read_only_and_requires_explicit_supersession_for_three_active_rows` proves the doctor changes neither source bytes nor file set and converges three active rows to one runnable row through append-only receipts. The stale-generation/tamper and wrong-lane/cross-campaign fixtures cover ledger and identity rejection. | Run the doctor against the stopped-state candidate copy, reconcile only after operator review, then include duplicate/restart/rollover T3 and preserve the receipt ledger in rollback evidence. |
| I37 | Every goal slice event is evaluated by one transition table before final-outbox selection. An active campaign may create at most one logical continuation child from a deterministic exact-lane intent committed before enqueue; restart/replay reconcile that same child, campaign slice generation never increments context-recovery depth, and acknowledgement occurs only after the child owns the runtime lane lease. An authoritative terminal campaign surface likewise commits one exact campaign/lane/virtual-session message and deterministic delivery identity before append; stale lineages, wrong sessions, and child/worker runs cannot own it. Every slice is bounded to 30–45 minutes and an append-only campaign ledger enforces wall-clock, slice-count, token-cost, repeated-no-progress, and recovery-count boundaries before continuation. | `goal_transition/goal_continuation/goal_budget/runtime_pipeline/context_rollover/runtime_worker/channel_delivery_index` | `goal_transition::tests::unified_transition_table_covers_all_incident_events` covers the event matrix. `goal_continuation::tests::intent_key_is_idempotent_and_slice_count_is_not_recovery_depth` and `committed_intent_reconciles_after_restart_without_duplicate_child` cover stable keys and the continuation commit/enqueue crash window. `runtime_pipeline::tests::active_goal_continuation_is_exactly_once_and_acknowledged_after_child_lease` runs two active slices, proves one child per intent, no final outbox, unchanged recovery depth, and post-lease acknowledgement. `active_goal_completed_slice_is_transitioned_before_final_outbox`, `paused_goal_emits_one_sanitized_terminal_notice`, and `goal_terminal_outbox_is_exactly_once_across_source_queues` cover older-completion suppression, sanitized terminal delivery, and append-before-receipt replay with one physical outbox row. `goal_budget::tests` covers the 48-hour/96-slice default, timeout clamping, idempotent cumulative ledger, and every breaker; `active_goal_budget_boundary_blocks_continuation_and_emits_one_notice` proves a production active slice cannot enqueue beyond its boundary. | Keep `goalAutonomy.mode=observe` in the candidate foundation. Before C2-G, select one exact lane digest, prove one runnable lineage, run restart/compact plus full wrong-lane/worker T3, and retain activation/rollback evidence. |
| I38 | Codex built-in web search is selected by explicit harness intent and exact-lane policy, never sandbox mode. Ordinary turns request cached, authorized freshness intent requests live, and sensitive/offline/replay turns force disabled. The same-connection provider `webSearch` capability gates the effective mode; capability absence is model-visible, and mode/provider/lane drift rolls over the thread. Receipts retain exact IDs and query digests without raw queries/private URLs, and untrusted results cannot enter memory, skills, or dream artifacts without their separate admission gates. | `codex_web_search/codex_runtime/config/memory/skill_episode/skill_learning` | Fixed classifier corpus; cached/live/capability-absent app-server boundary tests; matching-binding resume and legacy-binding rollover tests; action receipt query/private-URL leak scan; exact 0.144.5 `modelProvider/capabilities/read` staging probe. | Run the WEB T3 cached/live/disabled/capability-absent/restart corpus from the stopped-state candidate, retain receipt scans, and keep built-in live disabled on any lane requiring hard pre-query allowlists or exact query caps. |

## Continuity And External-Effect Bindings

The following bindings extend the named rows without changing their stable IDs:

- I2, I3, I7, and I9 require a typed distinction between logical success/failure, needs-user,
  continuation handoff, and terminal suppression. A parent handoff cannot render `Done` or emit the
  logical final; restart/compaction must retain its child link, and the eventual child owns the one
  final/error surface.
- I3, I5, I6, I7, I10, and I17 require structured provider failure plus mutation evidence before
  retry. A no-observable-mutation overload may persist a bounded same-request not-before schedule;
  mutation-observed recovery uses exact-lane continuation; unknown evidence fails closed. Retry
  lineage, attempt, delay, and eligibility remain reconstructable through restart and hot-ledger
  compaction without blocking another lane.
- I5, I7, I13, I17, I22, I35, and I37 bind deadline-drain task continuation to a current exact-lane
  OperationPlan, running/review item, typed bounded checkpoint, plan/item versions, and checksum.
  The intent commits before one deterministic child enqueue and revalidates stop, newer steer,
  budget, no-progress, depth, lane, version, and checksum before admission. Todo/ready/terminal or
  stale plans never become implicit autonomous Goals.
- I2, I3, I5, I6, I7, I9, I10, I17, I31, and I37 bind recognized MCP elicitations to exactly one
  protocol-valid response and an append-only external-effect state machine. Missing authority parks
  promptly as `WaitingForApproval`; approve/deny capabilities are exact-lane, action/parameter,
  expiry, and generation bound; stop/newer steer/expiry fence them. Restart from an intermediate
  `Requested` snapshot re-applies the same policy under the stable effect id before any submission.
  Submitted/ambiguous effects use
  an exact-bound GitHub opaque-marker or provider-idempotency readback adapter before any
  resubmission. Only a complete query can prove absence; mismatched or unprovable evidence needs
  user authority.
- I31 now fixture-locks the 0.144.5 MCP elicitation request/response shape in addition to the prior
  compact/goal/web-search surface. Generic receipts may retain effect id/state/digests, but never
  raw approval bearer tokens or raw connector parameters.
- I7 and I11 require channel adapter status/transport errors to retain only bounded redacted
  diagnostics. Absolute provider request URLs, URL userinfo, bot tokens, and credentials must not
  enter durable logs, receipts, or user-visible fallback text.

Round16-1 progress ordering binding: I9 requires progress delivery to preserve visible progress/final ordering by using queue-local wake freshness, continuing past stale non-terminal edits instead of dropping the whole pending batch, exempting first-surface sends from preemption, and reclaiming same-queue providerless surface claims without waiting for TTL. I17 requires progress delivery to consume durable terminal-control evidence before rendering cached non-terminal progress, emit at most one terminal close for existing provider surfaces, receipt `progressSuppressedReason=terminal-control-present`, and keep normal terminal cursor context intact for late-event monotonicity and retryable lane failures.

Cron incident binding: I17 also requires each deterministic occurrence to come from one exact crontab source, apply timezone/calendar and bounded per-entry execution policy before enqueue, carry stable occurrence metadata, and receipt explicit catch-up suppression. I3/I4 require already-exhausted jobs to terminalize before lease and deterministic timeouts to stop only the selected process tree.

Timeout-continuation binding: I3/I13 require sibling guards to resolve the effective queue state from trusted indexes and terminal controls instead of treating historical `status=queued` admission rows as live forever. I10/I13 require a productive absolute timeout to have concrete stdout evidence of tool/file-change activity or completed narration before it can become one guarded virtual-session continuation; handshake-only event volume remains ordinary retry/terminal evidence. I10 also requires the last 10% of a turn, capped at three minutes, to emit one receipted deadline-drain steer, avoid starting new long commands, and defer new user steering to next-turn/passive channel context. I20 requires automatic skill selection to have a structured lexical anchor rather than FTS/usage prior plus weak body-only coincidence, and runtime-merged duplicate `originalId` copies prefer the active workspace source when scores tie.

I38 release extension: the release-rule range is I1-I38. In addition to I27 progress blockers, a missing or mismatched canonical Codex provenance receipt, PATH/global-npm fallback, candidate/startup version/hash drift, protocol fixture drift, cross-binding/stale context capacity, a 120k failsafe that masks a known capacity, unattributed compact/goal completion, an unresolved/mismatched active goal lineage, a duplicate/unacknowledged continuation child, campaign-final leakage from an active slice, sandbox-derived or unexpected live web search, capability/mode mismatch, false online-verification claims, or raw search-query/private-URL leakage is a cutover blocker. This I1-I38 range supersedes the older I1-I27 wording retained in the detailed release-rule paragraph below.

Release rule: a regression that violates I1-I38 blocks cutover even if feature tests pass. I8 now includes `/new` as a prompt/memory task boundary, so fresh-task requests must not inherit previous-task imported memory context unless a future policy explicitly opts in. I9 now has focused completion-repair, stdout-recovery, structured worker-result redaction, read-only review suppression, owner/session mismatch suppression, and non-main owned-lane final-outbox routing gates; changes that touch compact fallback, fresh-thread recovery, provider delivery, or image/media tool completion still need the broader final-surface replay matrix before promotion. I10 is an initial guard, not full tool-supervisor parity; failed tool-timeout fresh-thread fallback may now route to virtual-session continuation, but future work should add per-tool cancellation and resumable execution before claiming the gap closed. I11 is deliberately generic: image-only compact or image-only prompt redaction is not enough when audio, browser, document, worker, provider-native media, generated media previews, Discord/TG attachments, referenced media, or large tool-log artifacts can enter durable context. I12 is the bridge ownership guard: configured openclaw-mem write/recall must be primary, recall fallback must stay read-only, and store fallback must fail closed. I13 is the context-rollover guard: official compact success must either advance rollover accounting or leave a structured skip receipt before long-context continuity can be considered reliable; polluted inline/native-image contexts must rotate before another doomed turn; polluted-thread, interrupted-long-task, or stream-unstable recovery must not double-deliver, strand sibling pending items, resurrect a parent after child enqueue, or suppress final max-depth failure; `maxContinuationDepth` terminal cases must record `terminal-failed`; and model-visible continuity must stay bounded to resolver pointers rather than payload replay. I14 is the presentation guard: rich Telegram/Discord output must be adapter-rendered, capability-gated, safely escaped, receipt-accounted with render mode/fallback/full-text preservation, preserve attachment-index/artifactRef media refs, and traceable as a batch before it can replace plain text final delivery. I15 is the channel-session-history guard: "previous session", virtual-session working context, and cross-session search/retrieval paths must not return concrete wrong-lane history from blended agent space; only exact-lane resolver evidence or explicit project/global scopes may cross continuity boundaries, and broad scopes are bounded by the harness-home store partition. I16 is the outbound media guard: parser expansion, artifactRef resolution, and provider-native upload changes must ship with the same policy gate, protected-span masking, loud rejection, and policy/lint receipts. I17 is the durable-control-artifact guard: queue-skip, scoped-stop, terminal run-once, quarantine, restart request, cron freshness, and sender source-receipt evidence must be checked before runtime execution, progress delivery, restart consumption, or sender-class cron notification; suppressions and consumptions must be idempotent and receipted, unresumable prepared executions must terminalize instead of churning, and stale source packets must suppress or explicitly mark force overrides. I18 is the delivery terminality guard: once any terminal receipt exists for a delivery id, later retryable audit receipts cannot resurrect it, and provider-permanent Telegram send failures must record terminal skip. I19 is the interruption taxonomy guard: `interrupted_by_new_turn` is resumable evidence, not a failed verification result. I20 is the skill ecosystem guard: synthesis is proposal-only by default, while any explicitly authorized apply must use the same lint/guard/proposal/checksum/backup receipt pipeline and never write directly. I21 makes the exact route and one unified reasoning state authoritative; aliases must not diverge and `ultra` cannot leak into normal effort/config surfaces. I22 makes prompt inventory, tombstones, `/new`, backend generation, and full-lane identity agent-scoped. I23 keeps every child policy independent while making terminal results internal, bounded master-owned evidence. I24 permits one durable continuation only after confirmed parent lease release and delays acknowledgement until that continuation owns the lease. I25 separates bounded interactive receipt reads from isolated retention. I26 requires Task Scheduler plan/reconcile parity so adding the maintenance owner cannot silently remove an enabled configured channel loop. I27 prevents a non-fresh source snapshot, historical providerless queue, or watched progress-owner stop from creating stale provider-visible progress or stranding fresh-send claims. Provider request acceleration changes must prove `/fast` command replies stay model-turn-free, Codex app-server `serviceTier` is gated by model-catalog `serviceTiers`, supported routes send only camelCase `serviceTier`, and unsupported/proxy/native routes receive no unverified acceleration fields. Codex/Node service executable and startup changes must prove spawn-stable native resolution and terminal startup/write failure receipts before cutover. Any new queue, tool, memory, or background-work path must either reuse these invariants or add a new invariant before release.
