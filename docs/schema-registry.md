# Agent Harness Schema Registry

Date: 2026-07-18

The authoritative in-code registry is `agent_harness_core::quality::schema_registry_entries`, exposed by `agent-harness schema-registry`. This document records the current public compatibility contract for review and release checks.

| Schema | Owner module | Compatibility rule | Current status |
|---|---|---|---|
| `agent-harness.runtime-run-once.v1` | `runtime_pipeline/workers` | Append-only JSONL; additive fields and statuses only in v1. Worker terminal reconciliation requires this exact schema plus queue-id and trusted route provenance. | Existing reader accepts legacy `timeout`; v1 adds typed `terminalDisposition`, `continuationLink`, `retrySchedule`, `taskDrainEvaluation`, and `externalEffect` fields plus `retry-pending`, nonterminal `auth-deferred`, `dead-letter`, `context-exhausted`, runtime metadata, and skipped cron tombstones. Active retry sequences remain hot through compaction so lineage, attempts, and eligibility are reconstructable; committed cold history stores nullable terminal/task/effect disposition and child-link fields. `auth-deferred` remains non-claimable, non-deliverable, and outside retry accounting until its deterministic wake. |
| `agent-harness.ledger-maintenance.v1` | `ledger_maintenance` | Read-only maintenance report; per-ledger compaction fields are additive in v1. Normal passes remain source-aware and bounded; force remains operator-only. | Terminal append paths only signal a coalesced wake. Runtime, channel-delivery, and progress retention execute under the isolated maintenance owner rather than the interactive ingress/completion/delivery path. |
| `agent-harness.codex-runtime-run.v1` | `codex_runtime` | Append-only JSONL plus per-execution JSON; additive fields only in v1. | Optional usage fields retain `modelContextWindow`, its source, provider/model route, backend context generation, and observation time. Bounded optional `protocolFailure`, `drainCheckpoint`, `externalEffect`, `toolUseTimeout`, and interruption fields preserve recovery authority without storing raw provider payloads. Productive timeout classification uses bounded stdout evidence rather than raw event count. |
| `agent-harness.codex-backend-provenance.v1` | `codex_backend_provenance` | Immutable candidate/startup receipt; additive metadata only in v1. Canonical path, exact version, executable SHA-256, and `probeResult=ready` remain mandatory. | Records the deployment-owned executable and npm/native package provenance, provider-scoped `CODEX_HOME` identity, config digest, and parent/child lifecycle correlation. A missing, non-canonical, out-of-root, wrong-version, or wrong-hash backend is a fail-closed candidate/startup error; PATH/global-npm fallback is not compatible. |
| `agent-harness.codex-protocol-compatibility.v1` | `codex_protocol_compat` | Version-locked sanitized fixture. Additive cases require regenerated 0.144.5 schema hashes; existing request/response/notification and harness-correlation expectations cannot be removed. | Covers initialization, thread start/resume, turn lifecycle, token usage and `modelContextWindow`, goals, compact acknowledgements/items, account lifecycle shapes, model/capability/config reads, web-search items, and error/null behavior. The compact/resume `interrupted:null` race remains a harness serialization/correlation responsibility. |
| `agent-harness.codex-web-search-decision.v1` | `codex_web_search/codex_runtime` | Per-execution receipt; requested/effective modes, intent/reason, provider/exact lane, capability generation, limitation, and sandbox independence are stable. Additive diagnostics only in v1. | Same-connection `modelProvider/capabilities/read` gates top-level thread `web_search`. Capability absence degrades to disabled with a model-visible no-online-verification limitation. |
| `agent-harness.codex-web-search-thread-binding.v1` | `codex_web_search/codex_runtime` | Append-only. Thread id, effective mode, provider, exact lane digest, policy digest, and capability generation are immutable in v1. | A missing or mismatched binding forces a fresh thread; matching bindings may resume. |
| `agent-harness.codex-web-search-action.v1` | `codex_web_search/codex_runtime` | Append-once event key. Exact thread/turn/item IDs, action, modes, capability generation, query digest, safe public domain, citation count, and non-admission classification are stable. Raw queries and private URLs are forbidden. | `webSearch` items are observable without copying prompt/query text into the receipt or admitting retrieved content into memory, skill, or dream state. |
| `agent-harness.backend-auth-state.v1` | `backend_auth` | Provider-scoped secret-free state plus append-only transition receipts. Lifecycle states and additive diagnostics may expand in v1; readiness generation, exact operation correlation, and credential-bearing field exclusion are stable. | `CODEX_HOME` is harness-owned and canonical. Browser/device/API-key input is operator-process-only; durable files may record readiness and redacted failure codes but never tokens, API keys, device/user codes, account identity, or complete auth URLs. |
| `agent-harness.codex-backend-auth.v1` | `backend_auth` | Append-only secret-free provider-home receipt. Provider-home digest, executable provenance receipt reference, lifecycle transition, selected operator method, one-way operation correlation, timestamps, redacted result, account/capability outcomes, and readiness generation are stable; additive diagnostics only in v1. | Operator start/completion and `account/read` + `model/list` reconciliation are correlated without raw login ids. A receipt cannot contain credentials, challenges, account identity, or complete authorization URLs. Runtime-ready requires both account and same-probe model capability evidence. |
| `agent-harness.backend-auth-continuation.v1` | `backend_auth/runtime_worker` | Provider- and exact-queue-scoped durable intent. `waiting`/`resumed`, the generation fence, and one resume are stable; diagnostics may be additive in v1. | An unready turn records one waiting intent. Reconciliation for a strictly newer `ready` generation appends one deterministic `retry-pending` wake before marking the intent resumed. Restart between those writes reuses the same `eventKey`, so it can finish the mark without losing or duplicating the wake. |
| `agent-harness.codex-active-turn.v1` | `codex_runtime` | Per-session active-turn binding; additive fields only in v1 and legacy bindings without deadline fields remain readable. | Adds optional `absoluteDeadlineAtMs` and `deadlineDrainAtMs` so channel steering can defer before the hard timeout without parsing process-local timers. |
| `agent-harness.codex-turn-steer-receipt.v1` | `codex_runtime` | Append-only steering receipts; status/delivery values are additive in v1. | Records queued/accepted/deferred user steering plus `sent-deadline-drain`, accepted/failed/unconfirmed drain outcomes, and `deferred-deadline-drain` next-turn routing. |
| `agent-harness.virtual-skill-manifest.v1` | `virtual_skill_manifest` | Versioned state artifact; additive fields only in v1. The delivery ledger is append-once and backward compatible when absent. | The independently gated F2 observer freezes catalog/topology/skill revisions per exact-lane virtual session, records delivery evidence without changing active serving, and treats `/new` as a terminal boundary. Integrated T3 remains required. |
| `agent-harness.skill-routing.v2` | `skill_router/skill_shadow_runtime` | Idempotent private receipt file; additive fields only in v2. | SK-F1 staging records exact-lane active-v4 IDs, shadow candidate-card IDs, hard-gate rejections, abstention, catalog digest, and query byte counts without raw task text. Shadow creates no delivery receipt or serving mutation. |
| `agent-harness.skill-delivery.v2` | `prompt/virtual_skill_manifest` | Additive fields only in v2; rehydration cannot link a fresh routing receipt. | The F2 runtime observer writes deterministic append-once first-load, explicit, rehydration, reference, or none receipts under a separate default-off flag. Rehydration preserves the frozen revision and has no fresh route link. |
| `agent-harness.skill-outcome.v1` | `skill_episode` | Additive fields only in v1; verified outcomes require bounded verifier references. | The runtime writer records ordinary model completion as `unknown`, never self-verifies it, and reserves verified success/failure for bounded external verifier evidence. |
| `agent-harness.skill-episode.v2` | `skill_episode` | Append-only exact-lane join; additive fields only in v2. | Joins selected-only/card/full-body/reference/rehydration delivery with outcome evidence without treating exposure or rehydration as positive use. |
| `agent-harness.skill-terminal-review.v1` | `skill_episode` | Append-once terminal review keyed by exact virtual session and terminal source. | Completion/dead-letter review evidence is retry-idempotent and cannot manufacture a second virtual-session learning review. |
| `agent-harness.skill-learning.v2` | `skill_learning/skill_episode` | Additive fields only in v2; learning requires joined episode, delivery, and outcome evidence. | The joined evidence path is implemented in source; activation remains proposal-only and requires integrated T3 before promotion. |
| `agent-harness.knowledge-classification.v1` | `knowledge_classifier` | Additive fields only in v1; one canonical disposition and explicit defer are stable. | Deterministic classification discards excluded sources, defers raw/ambiguous/contradictory evidence, and turns duplicates into references rather than mutable copies. |
| `agent-harness.skill-improvement-target.v1` | `skill_improvement` | Deterministic attribution receipt; additive reasons only in v1. | Explicit invocation, observed procedure, verifier/correction, and umbrella targeting are ordered; selected-only exposure cannot become a blame target and ambiguity abstains. |
| `agent-harness.skill-improvement-proposal.v1` | `skill_improvement` | Append-only checksummed proposal with immutable base/rollback/evidence fields. | Semantic patch and novelty synthesis outputs are reviewable, reversible, non-applied proposals. Synthesis requires two verified attributable episodes unless an explicit learn request authorizes one. |
| `agent-harness.skill-dream-run.v1` | `skill_dream` | Additive fields only in v1; report non-indexability and proposal-only status are stable. | Contract implemented; scheduled/runtime writer and T4 shadow soak remain open. |
| `agent-harness.skill-replay-manifest.v1` | `skill_replay` | Immutable named corpus manifest; relative fixture paths, SHA-256 checksums, case counts, and non-empty required-class owners are stable in v1. | F0 replay staging fails closed on path escape, checksum drift, case-count drift, and an unowned required evaluation class. |
| `agent-harness.skill-replay-case.v1` | `skill_replay` | Privacy-classified labeled fixture; additive labels and the `routingLabelsReviewed` boundary are stable in v1. | High-risk fixtures require two reviewers; public-mode loads reject `private-local` cases; unreviewed automatic-metric cases do not enter routing precision denominators. |
| `agent-harness.skill-replay-baseline.v1` | `skill_replay` | Immutable named metrics snapshot bound to one corpus checksum and policy revision; additive metric facets only in v1. | F0 baseline reporter produces deterministic routing, delivery-byte, duplicate-source, lane/class, and learning-target metrics. |
| `agent-harness.external-review-evidence.v1` | `codex_runtime` | Per-execution recovery artifact; additive fields only in v1. | Local Round10 gap closure captures review-only recovery output as evidence instead of final parent workflow completion. |
| `agent-harness.inbound-media-artifact.v1` | `media` | Artifact metadata is additive in v1; prompt-facing consumers must treat raw payload/provider fields as redaction candidates and use bounded extraction summaries for durable context. | Round10 adds optional `lifecycleStatus` and `extractionSummary` fields so images, audio/transcripts, generated media, browser captures, documents/downloads, large tool logs/review transcripts, worker reports, and provider-native attachments can be represented by refs plus summaries instead of raw blobs. Channel-media staging adds optional `provenance` so current-message and referenced-message media can be distinguished without exposing provider payloads. |
| `agent-harness.outbound-media-policy.v1` | `media_delivery_policy` | Append-only policy receipts; accepted extension/kind mappings may add entries in v1, while denied prefixes and max-byte checks must stay fail-closed. | Channel-media staging validates local path and harness-artifact outbound attachments before provider delivery and records accepted/rejected policy decisions. |
| `agent-harness.media-delivery-lint.v1` | `runtime_pipeline` | Append-only lint receipts; warning diagnostics are additive, and fail-closed mode must preserve terminal error semantics. | Channel-media staging masks protected spans before parsing media directives, records directive diagnostics, and can fail closed through `media.lintFailClosed`. |
| `agent-harness.runtime-dead-letter.v1` | `runtime_pipeline` | Additive fields only in v1; terminal receipt semantics are immutable. | Implemented in staging. |
| `agent-harness.runtime-queue-control.v1` | `runtime_queue` | Retry/skip receipts are append-only; terminal source ids are never mutated. | Implemented in staging. |
| `agent-harness.runtime-queue-quarantine.v1` | `runtime_worker` | Per-queue marker JSON is additive in v1 and rebuildable from terminalization evidence; presence is terminal control evidence. | Registered public contract. |
| `agent-harness.runtime-queue-leases.v1` | `runtime_worker` | Class-scoped state JSON accepts legacy `owner: "pid:<n>"` strings and structured owner envelopes in v1. | Implemented in staging. |
| `agent-harness.runtime-queue-lease-reconciliation.v1` | `runtime_worker` | Operator report for supervisor generation lease reaping; additive fields only in v1. | Implemented in staging. |
| `agent-harness.runtime-queue-latency.v1` | `latency` | Append-only per-stage queue latency receipts; additive stages and timestamps only in v1. | Implemented in staging. |
| `agent-harness.latency-status.v1` | `agent-harness-cli` | Read-only CLI summary over latency receipts; additive summary fields only in v1. | Implemented in staging. |
| `agent-harness.progress-delivery-state.v1` | `progress` | State JSON may add cursor/cache/counter fields in v1; existing lane cursors remain readable. | Round10 tracks body/status lane delivery counters; body/action lane non-terminal cap is finite while status/current-step heartbeat remains editable after body cap using `progressDeliveryStatusHeartbeatAfterBodyCapMs`. |
| `agent-harness.progress-delivery-plan.v1` | `progress` | Plan summary counters and warnings are additive in v1. Non-fresh source snapshots produce no provider pending items, and historical providerless queues remain suppressed. | The plan distinguishes deferred cached events from stale fresh-send suppression so operators can diagnose a safe defer without treating it as successful provider delivery. |
| `agent-harness.codex-context-preflight.v1` | `codex_runtime` | Append-only JSONL plus per-execution JSON; additive fields only in v1. | Records thread-health details plus effective model context window, capacity source/freshness/observation, ratio, decision, and policy. Fresh exact binding/route/generation capacity and static route capacity are evaluated before the 120k unknown/stale failsafe. |
| `agent-harness.codex-resume-settle.v1` | `codex_runtime` | Append-only JSONL; additive observations and statuses only in v1. Exact lane digest, thread ID, and backend generation are immutable correlation axes. | Records the bounded post-resume quiet/maximum settle window, restored usage/goal/thread/turn observations, and whether a restored goal turn was drained before same-thread maintenance. Restored-turn output remains internal continuity evidence rather than queued-turn final output. |
| `agent-harness.compact-attempt.v1` | `codex_runtime/context_rollover` | Append-only lifecycle snapshots. `planned`, `rpc-acknowledged`, `compact-item-started`, `compacted`, `failed`, and `ambiguous` are stable states; observations are additive in v1. | Binds compact RPC request ID to exact thread, lane/backend generation when available, resume-settle/active-goal state, request/ack times, observed turn/item IDs and event classifications, public status/error, recoverable core abort detail, and final classification. Unrelated `turn/completed` never closes compact; `error:null` is absent and death after ack remains ambiguous. |
| `agent-harness.codex-goal-projection.v1` | `codex_runtime/context_rollover` | Append-only exact-source-thread projection. Objective/status/token budget, completion criteria/checksums, stable goal reference, optional backend goal/turn references, observation order, and complete/incomplete classification are immutable in v1. | One protocol-state observer captures `thread/goal/updated` across normal turns, resume settle, verified rehydration, compact/capability waits, and stdout crash recovery. Status-only events reuse the same-thread exact prior projection; missing prior data is persisted as fail-closed incomplete evidence. Latest terminal status cannot resurrect an older active row. |
| `agent-harness.codex-goal-rehydration.v1` | `codex_runtime/context_rollover` | Append-only lifecycle snapshots. Replacement thread/generation, source projection/checksums, checkpoint checksum, echoed goal checksum/status, and the verified gate are stable in v1. | A fresh fallback thread cannot receive the next explicit turn until `thread/goal/set` is acknowledged and `thread/goal/updated(active)` echoes the same backend goal checksum. An automatically started goal turn is drained as the released continuation instead of sending a competing explicit turn. |
| `agent-harness.working-set-memory.v2` | `context_rollover/virtual_session_context` | Exact `ChannelStateLane` metadata is required; bounded working-set fields may be additive in v2. Legacy v1 is migration input, not write authority. | Production completion writes v2 only. Reads validate platform/account/channel/user/agent; prompt resolution exposes an opaque reference rather than a local absolute working-set path. |
| `agent-harness.working-set-session-index.v2` | `context_rollover/virtual_session_context` | Exact platform/account/channel/user/agent/session identity is immutable; index metadata may be additive in v2. | The storage key is a SHA-256 over every normalized lane axis plus concrete session. Non-default lanes never read legacy accountless indexes. |
| `agent-harness.virtual-session.v2` | `context_rollover/virtual_session_context` | Exact account-aware lane identity, root/concrete sessions, and monotonic continuation state are immutable; lifecycle metadata may be additive in v2. | Default-account v1 migration is explicit and requires exact matches on every other axis; unknown axes never wildcard-match. |
| `agent-harness.virtual-session-authority.v1` | `runtime_pipeline/context_rollover` | Append-only production projection. Full-lane digest, virtual/concrete session, working-set file, and backend generation binding are immutable in v1. | Every completed production v2 snapshot records whether a trusted backend generation was available. `generation-unbound` is diagnostic fail-closed evidence and cannot authorize autonomous continuation. |
| `agent-harness.goal-lineage.v1` | `goal_lineage` | Read-only derived projection. Campaign family, exact lane, virtual session, backend generation, source thread/turn, goal reference, checksums, and observation order are immutable identity axes in v1. | Joins backend goal projections to the latest exact-v2 authority for the same queue/session. Objective text stays in private source state; operator reports expose checksums and bounded reasons. |
| `agent-harness.goal-lineage-doctor.v1` | `goal_lineage/agent-harness-cli` | Read-only report; findings/counters may be additive in v1. Running the doctor never writes a receipt or repairs source rows. | Detects multiple active rows, wrong-lane/missing authority, stale generation, malformed input, and conflicting supersession. Filters are exact lane and virtual session only. |
| `agent-harness.goal-lineage-supersession.v1` | `goal_lineage` | Append-only reviewed reconciliation. Winner, loser, campaign family, exact lane, and virtual session are immutable; backend goal rows are never deleted. | The writer verifies both rows belong to one campaign and that the winner is current/runnable. A deterministic receipt ID makes repeated identical reconciliation idempotent. |
| `agent-harness.goal-transition.v1` | `goal_transition/runtime_pipeline` | Append-only unified decision receipt. Exact goal/lane/session/generation/source-slice identity and decision generation are immutable in v1. | Normal completion, deadline drain, timeout, compact rollover, auth defer, restart, operator stop, newer steer, older-lineage completion, and runtime failure use one decision table before final-outbox selection. Replaying the same source slice reuses its prior transition. |
| `agent-harness.goal-slice.v1` | `goal_transition/runtime_pipeline` | Logical campaign-slice identity. Campaign slice generation remains independent from context recovery continuation depth. | Active campaign slices surface progress only; only authoritative terminal goal state may own the campaign final. |
| `agent-harness.goal-continuation-intent.v1` | `goal_continuation/runtime_pipeline` | Append-only commit/enqueue/ack lifecycle shared by Goal, OperationPlan deadline-drain, and external-effect continuations. The deterministic intent key and exact authority axes are immutable; one intent owns at most one child. | Intent is committed before enqueue, restart reconciles a committed or already-enqueued child, and acknowledgement occurs only after the child owns the runtime lane lease. Goal autonomy remains separately gated; typed task/effect authority cannot be substituted by an active Goal string. |
| `agent-harness.task-continuation-checkpoint.v1` | `task_transition/codex_runtime` | Bounded typed assistant marker. Authority kind/id/version, active item id/version, checkpoint body, and lowercase SHA-256 digest are immutable. | Accepted only on a completed deadline-drain turn. The marker is verified and removed from visible assistant output; malformed, oversized, stale, wrong-lane, or ordinary-completion markers fail closed. |
| `agent-harness.task-transition.v1` | `task_transition/runtime_pipeline` | Additive drain evaluation embedded in runtime receipts; exact-lane authority snapshot and breaker disposition are immutable for one source completion. | An open exact-lane OperationPlan with a running/review item and current checkpoint may require one deterministic child. Todo/ready/terminal plans and stop/newer-steer/budget/no-progress/depth/version breakers do not auto-loop. |
| `agent-harness.external-effect-intent.v1` | `external_effect/codex_runtime/runtime_pipeline` | Append-only effect generation keyed by exact lane, logical lineage, source queue, connector/action, and parameter digest. State transitions are monotonic and fenced; generic serialization excludes bearer approval tokens. | Requested/approval-required/approved/denied/submitted/confirmed/ambiguous states drive protocol response, progress, mutation evidence, readback, and recovery. Submitted or ambiguous mutation is never blindly replayed. |
| `agent-harness.external-effect-transition.v1` | `external_effect` | Append-only transition evidence. Effect id, prior/next state, exact lane digest, and parameter digest remain stable; bounded reasons are additive. | Restart/replay rebuilds the latest intent from protected snapshots and transition evidence without manufacturing another mutation. |
| `agent-harness.connector-approval-token.v1` | `external_effect/channel_runtime` | Protected latest-state capability only. Token digest, exact lane/action/parameter binding, expiry, and consumption are immutable; raw bearer values are forbidden from generic receipts and public serialization. | `/approve <token>` and `/deny <token>` are provider-neutral exact-lane commands. Same-decision replay is idempotent; wrong lane, opposite decision, expiry, stop, or newer steer fails closed or fences the capability. |
| `agent-harness.github-issue-readback-evidence.v1` | `external_effect` | Bounded connector-specific readback evidence contains only the approved parameter digest, a query-completeness bit, and opaque effect markers; issue titles, bodies, URLs, ids, and account metadata are forbidden. | The exact stable marker proves presence. Only a complete exact-scope query may prove absence; partial lookup remains unprovable and cannot authorize resubmission. |
| `agent-harness.provider-idempotency-readback-evidence.v1` | `external_effect` | Bounded provider readback evidence is immutable-bound to connector, action, parameter digest, and the stable effect-derived idempotency key. | Presence/absence/unknown maps to confirmed/one bounded retry/ambiguous only after every binding matches. A mismatch fails closed rather than being treated as absence. |
| `agent-harness.goal-terminal-outbox.v1` | `runtime_pipeline` | Append-only committed/appended campaign-terminal surface receipt. Terminal key, deterministic delivery ID, exact campaign/lane/virtual-session authority, decision, and immutable canonical outbound message are fixed by the first commit. | The campaign-terminal lock commits before append. Restart or a different slice reuses the canonical message and delivery ID, repairing the append-before-receipt window without a duplicate, late, worker-owned, or wrong-lane final. |
| `agent-harness.goal-campaign-budget.v1` | `goal_budget/runtime_pipeline` | Append-only per-slice campaign budget receipt. Exact campaign/lane/virtual-session identity, source slice, effective policy, cumulative counters, progress fingerprint, and selected boundary are immutable. | Idempotent queue/slice replay reuses the prior receipt. Wall-clock, slice-count, token-cost, no-progress, and recovery boundaries feed the unified transition before continuation scheduling. |
| `agent-harness.goal-campaign-status.v1` | `goal_budget/agent-harness-cli` | Read-only effective-policy and latest-per-campaign status report; additive report fields only in v1. | `goal-campaign-status` reads configuration and budget receipts without mutating either input. It exposes hashes/counters and exact policy values, not prompt or credential payloads. |
| `agent-harness.codex-context-checkpoint.v1` | `codex_runtime` | Per-execution recovery artifact; additive fields only in v1. | Implemented in staging. |
| `agent-harness.codex-context-rollover.v1` | `codex_runtime` | Per-execution recovery artifact; binding backup path remains optional. | Implemented in staging. |
| `agent-harness.virtual-session-working-context.v1` | `virtual_session_context` | Read-only bounded resolver envelope over exact-lane working-set/session receipts; additive fields only in v1 and evidence anchors remain pointers, not payloads. | Implemented in staging. |
| `agent-harness.channel-identity-check.v1` | `channel_identity` | Additive fields only in v1; non-bound statuses remain fail-closed. | Implemented. |
| `agent-harness.channel-identity-registry.v1` | `channel_identity` | Additive binding fields only in v1; ambiguous bindings must fail closed. | Implemented. |
| `agent-harness.channel-delivery-intent.v1` | `channel_runtime` | Additive fields only in v1; provider ids must come from captured inbound context. | Implemented. |
| `agent-harness.channel-delivery-receipt.v1` | `channel_delivery` | Append-only delivery receipts; `presentation`, `renderedUnits`, and `renderedUnits.attachmentKind` are additive, and legacy receipts without presentation remain readable. | Registered public contract. |
| `agent-harness.rich-message-presentation.v1` | `rich_presentation` | Optional outbound presentation field; old outbox JSON without `presentation` must deserialize as plain text. v1 accepts additive semantic fields only after validation. | Package A adds schema structs, fail-closed validation, backward-compatibility coverage, and render-only Telegram/Discord fixtures; Package B adds rendered batch units and action capability gates; Package C stages provider send helper integration for adapter-rendered Telegram/Discord text/media plus rendered-unit delivery receipts while callbacks remain disabled. Channel-media staging resolves inbound-media and generated-image/generated-media `artifactRef` units into policy-validated attachments and records `renderedUnits.attachmentKind`. |
| `agent-harness.channel-restart-request.v1` | `channel_runtime` | Restart request receipts are append-only; stop-file envelope action fields are additive in v1. | Implemented in staging. |
| `agent-harness.gateway-restart-request.v1` | `channel_state` | Protected gateway restart requests are append-only; plain `/restart` remains request-only in v1. | Implemented in staging. |
| `agent-harness.cron-scheduler.run-once.v1` | `cron_scheduler` | Additive fields only in v1; dry-run must not enqueue or write watermarks. | Implemented. |
| `agent-harness.cron-scheduler.lint.v1` | `cron_scheduler` | Additive diagnostics only in v1; error status remains fail-closed. | Implemented in staging. |
| `agent-harness.cron-scheduler.tick.v1` | `cron_scheduler` | Append-only receipts; additive fields only in v1. | Implemented. |
| `agent-harness.cron-scheduler.job-decision.v1` | `cron_scheduler` | Append-only receipts; idempotency key semantics are stable in v1; retry attempts may add an `:attempt:<n>` suffix. | Implemented. |
| `agent-harness.cron-runs.v1` | `cron_runs` | SQLite state table; additive columns only in v1; status enum changes require migration. | Implemented in staging. |
| `agent-harness.config-validation.v1` | `config` | Additive diagnostics only in v1; invalid config remains fail-closed. | Implemented in staging. |
| `agent-harness.log-rotation.v1` | `logging` | Additive fields only in v1; rotation receipts are append-only. | Implemented in staging. |
| `agent-harness.supervision-evaluation.v1` | `supervision` | Additive child fields only in v1. | Implemented in staging. |
| `agent-harness.supervisor-stop-file.v1` | `ops` | JSON stop-file envelope may add metadata in v1; legacy plain-text reasons stay readable. | Implemented in staging. |
| `agent-harness.runtime-loop-runner-safe-mode.v1` | `supervisor` | Runner safe-mode JSON may add diagnostic fields in v1; `restartAfterSeconds` and `memoryGateDecision` remain advisory. | Implemented in staging. |
| `agent-harness.supervisor-service-state.v1` | `supervisor` | Service state JSON may add diagnostic fields in v1; `launchOwner` and `servicePriority` distinguish observe-only external runners from `rust-supervisor-run` children. | Implemented in staging. |
| `agent-harness.supervisor-inventory.v1` | `supervisor_inventory` | Desired-service inventory reports may add health fields in v1; missing/stale/launch action semantics remain stable. | Implemented in staging. |
| `agent-harness.supervisor-reconcile.v1` | `agent-harness-cli` | CLI reconcile output may add launch diagnostics in v1; apply remains explicit and never implied by dry-run. | Implemented in staging. |
| `agent-harness.healthz.v1` | `health` | Local/admin JSON status; additive fields only in v1. | Implemented in staging. |
| `agent-harness.trace.v1` | `trace` | Additive record fields only in v1. | Implemented in staging. |
| `agent-harness.metrics.v1` | `metrics` | Counter names are stable once published; new counters may be added. | Implemented in staging. |
| `agent-harness.supervise-deploy-canary.v1` | `deploy` | Additive fields only in v1; `commit`/`rollback` decisions remain stable. | Implemented in staging. |
| `agent-harness.queue-shadow-record.v1` | `queue_shadow` | Additive fields only in v1. | Implemented in staging. |
| `agent-harness.queue-shadow-compare.v1` | `queue_shadow` | Additive divergence fields only in v1. | Implemented in staging. |
| `agent-harness.admission-decision.v1` | `admission` | Additive fields only in v1; refusal must remain explicit. | Implemented in staging. |
| `agent-harness.scoped-stop.v1` | `admission` | Target shape is stable in v1; add new target kinds only with compatibility tests. | Implemented in staging. |
| `agent-harness.background-registry.v1` | `background` | SQLite task JSON may add fields in v1; status enum changes require migration. | Implemented in staging. |
| `agent-harness.token-efficiency.v1` | `token_efficiency` | Additive fields only in v1. | Implemented in staging. |
| `agent-harness.wake-sequence.v1` | `wake` | Per-lane wake sequence files may add diagnostic fields in v1; sequence remains monotonic best-effort. | Implemented in staging. |
| `agent-harness.task-entity.v1` | `autonomy` | Additive fields only in v1; checkpoint JSONL remains append-only. | Implemented in staging. |
| `agent-harness.budget-decision.v1` | `autonomy` | Additive fields only in v1; accepted/blocked decision semantics remain stable. | Implemented in staging. |
| `agent-harness.learning-proposal.v1` | `autonomy` | Proposal JSON may add review fields in v1; auto-apply remains opt-in. | Implemented in staging. |
| `agent-harness.operation-plan.v1` | `operation_plan` | Plan JSON may add metadata in v1; plan id and status semantics remain stable. | Implemented in staging. |
| `agent-harness.operation-plan-item.v1` | `operation_plan` | Item JSON may add metadata in v1; evidence-required completion remains stable. | Implemented in staging. |
| `agent-harness.operation-plan-event.v1` | `operation_plan` | Append-only plan event records; additive fields only in v1. | Implemented in staging. |
| `agent-harness.operation-plan-comment.v1` | `operation_plan` | Append-only comments; additive fields only in v1. | Implemented in staging. |
| `agent-harness.operation-plan-receipt.v1` | `operation_plan` | Append-only receipts; idempotency keys and action names remain stable in v1. | Implemented in staging. |
| `agent-harness.builtin-skill-sync.v1` | `harness_skills` | Builtin skill sync receipts are additive; user-modified skills remain protected unless forced. | Skill ecosystem staged contract. |
| `agent-harness.builtin-skill-manifest.v1` | `harness_skills` | Builtin manifest entries keep skill id, path, version, and fingerprint stable in v1. | Skill ecosystem staged contract. |
| `agent-harness.skill-index.v1` | `skills` | Skill index output may add summary facets and additive frontmatter fields such as `agents`; skill ids, source kinds, paths, checksums, and frontmatter fields remain stable evidence anchors. | Runtime turn planning builds the source portion from the selected agent home/workspace before merging shared harness namespaces. |
| `agent-harness.skill-invocation-envelope.v1` | `skill_envelope` | Byte-framed envelope with declared lengths/checksum; parser must ignore nested sentinel text inside body bytes. | Implemented in staging. |
| `agent-harness.skill-selection.v1` | `skills` | Append-only selection receipts with matcher metadata, delivery mode, body checksum, score components, selected `agentId`, and deterministic tie-breaks. | Matcher v4 uses tokenizer `mixed-v1`, preserves CJK/tag/category/FTS/usage-prior scoring, enforces agent allowlist/lifecycle/invocation gates, requires a structured lexical anchor for automatic selection, and deduplicates runtime-merged copies by `originalId` with active-source preference on ties without a schema break. |
| `agent-harness.prompt-injection-ledger.v2` | `prompt` | V2 skill entries are keyed by session, agent, skill id, body checksum, and delivery mode; v1 path/fingerprint ledgers remain readable for migration. | Implemented in staging. |
| `agent-harness.skill-usage.v1` | `skill_usage` | Append-only skill usage/provenance JSONL; action enum additions require status and curator compatibility tests. | Implemented in staging. |
| `agent-harness.skill-usage-snapshot.v1` | `skill_usage` | Derived compact status/curator artifact rebuildable from `skill-usage.jsonl`. | The stored aggregate remains diagnostic; turn scoring derives an exact-`agentId` filtered view so usage priors do not cross agents. |
| `agent-harness.skill-lint-receipt.v1` | `skill_lint` | Append-only lint receipts; finding codes and severities are additive in v1. | Skill ecosystem staged contract. |
| `agent-harness.skill-guard-receipt.v1` | `skill_guard` | Append-only guard receipts; verdict semantics safe/caution/dangerous remain stable in v1. | Skill ecosystem staged contract. |
| `agent-harness.skill-lifecycle.v1` | `skill_curator` | State JSON may add lifecycle metadata in v1; archive remains restorable move semantics. | Skill ecosystem staged contract. |
| `agent-harness.skill-curator.v1` | `skill_curator` | Per-run reports are additive; dry-run must not mutate skill files. | Skill ecosystem staged contract. |
| `agent-harness.skill-restore.v1` | `skill_curator` | Restore receipts are additive; archive source and restored target paths remain stable in v1. | Skill ecosystem staged contract. |
| `agent-harness.skill-pin.v1` | `skill_curator` | Pin receipts are additive; pinned/unpinned state remains the lifecycle protection contract in v1. | Skill ecosystem staged contract. |
| `agent-harness.skill-pack.v1` | `skill_pack` | Pack manifest is checksum-guarded; additive manifest fields are allowed in v1. | Skill ecosystem staged contract. |
| `agent-harness.skill-pack-lock.v1` | `skill_pack` | Lockfile may add metadata in v1; installed path checksum entries remain stable. | Skill ecosystem staged contract. |
| `agent-harness.skill-pack-receipt.v1` | `skill_pack` | Import/export/remove receipts are additive; installed path and checksum evidence remains stable in v1. | Skill ecosystem staged contract. |
| `agent-harness.skill-synthesis-receipt.v1` | `skill_synthesis` | Append-only synthesis receipts; proposal ids and target paths are stable evidence anchors. | Skill ecosystem staged contract. |
| `agent-harness.skill-autonomous-apply-receipt.v1` | `skill_apply` | Append-only autonomous review receipts; approve, quarantine, and blocked decisions are stable in v1. | Retained for explicitly authorized apply flows. CLI and worker synthesis now default to proposal-only; apply requires the explicit CLI `--apply` path or worker authorization. |
| `agent-harness.skill-proposal.v1` | `skill_learning` | Append-only proposal state records; apply remains checksum-guarded and explicitly authorized. | Implemented in staging; proposal persistence alone never mutates a skill. |
| `agent-harness.skill-apply-receipt.v1` | `skill_apply` | Append-only apply receipts; stale-base quarantine and backup-before-mutation semantics are stable in v1. | Implemented in staging. |
| `agent-harness.skill-doctor.v1` | `skill_doctor` | Aggregate health reports are additive in v1; non-receipt read-only runs preserve report shape. | Skill ecosystem staged contract. |
| `agent-harness.learning-review.v1` | `skill_learning` | Deterministic learning-review report; worker jobs may propose but never mutate skill files directly. | Implemented in staging. |
| `agent-harness.self-improvement-review.v1` | `self_improvement` | Append-only review hook receipts; apply mode aliases are additive and replacements remain checksum-guarded. | Implemented in staging. |
| `agent-harness.drift-report.v1` | `autonomy` | Additive fields only in v1. | Implemented in staging. |
| `agent-harness.context-pack.v1` | `memory_contracts` | Canonical normalized memory context pack; breaking changes require v2 and fail-open consumer tests. | Implemented in staging. |
| `openclaw-mem.context-pack.v1` | `memory_contracts` | Accepted producer schema translated to `agent-harness.context-pack.v1`; unknown versions fail open. | Implemented in staging. |
| `agent-harness.openclaw-mem-local-owner-prepare.v1` | `memory` | Append-only receipts; local prepare may add diagnostics but must not promote without operator approval. | Implemented in staging. |
| `openclaw.mem.dream-director.send-receipt.v1` | `dream_director` | Additive receipt fields only in v1; stale-source suppression remains fail-closed unless a force override is receipted. | Registered public contract. |
| `agent-harness.encrypted-vault.v1` | `vault` | Breaking crypto/KDF changes require v2 and migration receipt. | Implemented in staging. |
| `agent-harness.security-scan.v1` | `security` | Additive findings only in v1. | Implemented in staging. |
| `agent-harness.runtime-queue-item.v2` | `runtime_queue/runtime_worker/workers` | Flat additive queue wire. Coordinator-resume metadata is a typed V2 variant; execution snapshots require immutable `admissionQueueId` and `authorizedExecutionMode` together. V1 readers remain supported by v0.8.0. | Registered v0.8.0 contract. A v0.7 reader is not guaranteed to accept V2 state. |
| `agent-harness.agent-prompt-manifest.v1` | `prompt` | Per-agent canonical static-source inventory. Alias resolution, tombstones, backend generation, exact-lane digest, and canonical `MEMORY.md` entries are additive in v1; dynamic recall remains outside the manifest. | Governed by scenario `per-agent-prompt-manifest`. |
| `agent-harness.child-execution-policy.v1` | `child_execution_policy/workers` | Immutable per-child provider/model/reasoning snapshot. Open-ended canonical effort strings are additive and must be validated against the resolved route; exact `ultra` is not an effort. | Governed by scenario `master-owned-subagent-coordination`. |
| `agent-harness.child-execution-policy.v2` | `child_execution_policy/workers` | Additive wrapper over V1 with an optional default-off execution snapshot. Reasoning effort never accepts reserved execution-mode values. | Registered v0.8.0 contract; no public execution-mode configuration is advertised. |
| `agent-harness.worker-result-owner.v1` | `worker_result_mailbox` | Exact full-lane, virtual-session, parent/source, and optional plan identity are immutable. Legacy incomplete owners remain auditable but cannot auto-resume. | Registered v0.8.0 contract. |
| `agent-harness.worker-result-envelope.v1` | `worker_result_mailbox` | The actual terminal outcome is represented only by a redacted summary of at most 4 KiB and up to 16 opaque artifact pointers. Additive outcome metadata must preserve the content policy; raw prompts/provider events, credentials, and absolute paths are forbidden. | Registered v0.8.0 contract. |
| `agent-harness.worker-result-mailbox.v1` | `worker_result_mailbox/workers` | SQLite append-once terminal events with additive columns only. Owner rebinding conflicts fail closed and acknowledgement is monotonic. | Registered v0.8.0 contract. Expected children lacking valid exact-owner evidence receive deterministic failed-omission envelopes before resume. |
| `agent-harness.worker-coordinator-wait.v1` | `worker_coordination/worker_adapters` | SQLite parent-wait state with immutable exact owner and child set. State transitions are monotonic and duplicate admission is idempotent. | Registered v0.8.0 contract. |
| `agent-harness.worker-resume-intent.v1` | `worker_resume` | SQLite exact-lane resume intent. Sequence is monotonic, duplicate result sets coalesce, expired claims are restart-reclaimable, and any active exact-lane lease blocks scheduling. | Registered v0.8.0 contract. |
| `agent-harness.coordinator-resume.v1` | `coordinator_resume/workers/runtime_worker` | Typed continuation metadata binds wait, intent, exact owner, and deterministic continuation queue id. Additive fields only in v1. | Registered v0.8.0 contract. Coordinator model context contains only validated bounded/redacted mailbox envelopes, opaque references, and structural child metadata. |
| `agent-harness.safe-resume-readiness.v1` | `execution_mode/runtime_queue` | Readiness evidence is observational and fail-closed. Additive probes may be added without treating caller booleans as authority. | Registered v0.8.0 internal compatibility contract. |
| `agent-harness.quality-report.v1` | `quality` | Additive fields only in v1. | Implemented in staging. |
| `agent-harness.scenario-matrix.v1` | `quality` | Release-gate catalog; additive scenario entries, evidence fields, and runnable-test pointers only in v1. | v0.8.0 registers exactly `gpt56-reasoning-capability`, `per-agent-prompt-manifest`, and `master-owned-subagent-coordination` for this change set. Registration is not verification evidence. |

Release rule: every new receipt/state schema must be added here and to `schema_registry_entries` before release. Breaking changes require a V2 schema, old-version reader, and migration receipt for one release cycle. A v0.8.0 reader accepting a legacy artifact is a forward-migration promise only; it does not mean a v0.7 binary can safely read state already written by v0.8.0.
