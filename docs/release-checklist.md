# Agent Harness Release Checklist

Date: 2026-06-29

This checklist is mirrored by `agent-harness release-checklist`. It is intentionally review-oriented: passing it should leave evidence that maps to the external review dimensions.

## Required Before Any Live Cutover

- `cargo fmt --all`
- `cargo test --workspace --target-dir target\staging-test-workspace`
- `agent-harness config-validate --target-home <staging-home>`
- `agent-harness healthz --target-home <staging-home> --require-writable-state`
- `agent-harness metrics --target-home <staging-home>`
- `agent-harness schema-registry`
- `agent-harness invariants`
- `agent-harness cron-runs --target-home <staging-home> --limit 20`
- `agent-harness status --target-home <staging-home> --json` reviewed for runtime class queues/open counts, class leases, CronRun summary, and cron scheduler tick summaries.
- For cron/runtime changes, targeted tests must cover native cron admission, retry watermark recovery, worker pre-runtime failure sync, worker/runtime CronRun control blockers, skipped cron runtime tombstones, one-shot/sticky cron namespace isolation, dedicated cron worker/runtime lanes, and legacy root runtime lease compatibility.
- `agent-harness public-hygiene --root <public-export-root>`
- Changelog entry updated.
- Schema registry updated for every new receipt/state schema.
- Topology contract impact matrix reviewed for changed modules: `docs/agent-harness-topology-contract.md`.
- Channel/runtime/session changes passed the agent-boundary scenario pack from `docs/agent-harness-topology-contract.md`; include same-agent stale suppression and different-agent non-suppression evidence.
- Prompt/memory/session changes passed fresh `/new` task-boundary and per-agent recall compartment checks; include `prompt::tests::prompt_bundle_new_command_boundary_skips_prior_task_memory_context` and `memory::tests::non_main_memory_prompt_context_excludes_global_imported_snapshot_by_default` or a documented stronger equivalent.
- Docs/skills/help stale-guidance review completed: update or remove outdated, misleading, or ambiguous instructions in public docs, generated CLI help, builtin harness skills, feature parity HTML/Markdown, and operational runbooks. Do not satisfy this gate by only adding new notes while leaving old contradictory guidance in place.
- Behavior-changing response/runtime policies verified in code, docs, skill text, CLI help, and live config: response tone defaults, final-reply wrappers, final-surface separation, progress text caps and edit-volume caps, retry/dead-letter classifiers, and session-preserving recovery notes must agree.
- Response/runtime changes passed final-surface separation checks: when `assistantNarrationMode=progress_panel`, progress or narration stream text must remain in progress/audit receipts and must not be written to final `agent-reply` outbox payloads. Use `docs/agent-harness-assistant-narration-tech-note.md` as the behavior reference, include stdout recovery without `final_answer`, and include compact-before-turn fallback or fresh-thread recovery in the replay matrix when the change touches Codex completion recovery.
- Progress delivery changes passed edit-volume replay checks: for `progress-delivery-volume-gap`, long-running turns must prove bounded per-queue body/status edits, repeated text-hash suppression, terminal convergence, final-outbox presence, and no post-terminal edit churn across Telegram and Discord.
- Progress panel lane-cap heartbeat checks passed across channel platforms: the body/action lane may hit a finite non-terminal cap, but the status/current-step lane must continue low-frequency heartbeat edits after `progressDeliveryStatusHeartbeatAfterBodyCapMs`, update immediately when a new assistant execution summary/current step is appended, include `Updates capped; still working.` plus latest internal event time, dedupe identical heartbeat text, and bypass caps for terminal `Done` / `Failed`.
- Codex tool-use timeout changes passed bounded recovery checks: active tool-use idle timeout must stop the stalled app-server/tool path, record `toolUseTimeout`, and either recover through one bounded continuation or produce an explicit terminal trace. The Round10 guard is initial coverage, not full per-tool supervisor parity.
- External review loop changes passed resumability checks: Claude/second-brain review output, timeout, or auth failure must be recorded as review evidence/progress and must not close the parent workflow unless the parent queue explicitly reaches final completion or a terminal blocker notification.
- Artifact/context hygiene changes passed generic artifact prompt/progress redaction and Discord attachment extraction checks: images, audio/voice memo/transcripts, generated speech/media, video thumbnails, browser captures, downloads/documents, large tool logs/review transcripts, worker datasets/reports, provider-native media attachments, and Discord text/image attachments must enter main-session prompt/continuity/progress surfaces as artifact references plus bounded extraction summaries only. Raw bytes, base64, provider URLs, Telegram `file_id`, Discord attachment URLs, cookies/tokens, document bodies, and large tool blobs must remain in artifact storage or receipts. Discord attachment download changes must also prove non-Discord/provider-internal URLs fail closed before fetch, download-failure warnings do not persist raw provider URLs/tokens, declared image attachments require supported image bytes before storage, and JSON-like attachments get prompt-safe `.json` artifact paths.
- Rollback note recorded for the deployment candidate.
- Staging trace sample captured for normal, canceled, timeout, and dead-letter paths.

## Required Before Production-Complete Roadmap Claims

- Seven-day queue shadow summary with zero unexplained divergence before P2 cutover.
- WinSW/service-wrapper restart, ordered shutdown, and reboot recovery receipts before unattended-operation claims.
- Telegram, Discord, and OpenRouter smoke receipts before live channel/provider claims.
- Live or fake-only canary deploy receipt for the exact candidate binary before cutover.
- Vault migration, restore, and rotation receipts before long-lived plaintext secret stores are retired.
- Network-backed dependency advisory audit when an advisory database tool is available.
- Sanitized scenario replay corpus and restore drill receipt before P7 quality/maturity claims.

## Last Verified Staging Evidence

- 2026-06-28: Round10 staging work added focused gates for `progress-final-surface-gap` and `progress-delivery-volume-gap`. `runtime_pipeline::tests::already_recorded_completion_repair_keeps_progress_panel_out_of_final_outbox` and `runtime_pipeline::tests::already_recorded_completion_repair_keeps_progress_panel_out_of_discord_final_outbox` prove already-recorded completion repair keeps progress-panel narration out of final outbox payloads for Telegram and Discord fixtures. `progress::tests::delivery_plan_volume_limits_nonterminal_updates_but_allows_terminal_convergence` and `progress::tests::delivery_plan_reads_volume_limit_from_response_config` prove bounded non-terminal body/status progress updates and terminal convergence. The broader live Telegram/Discord replay matrix remains the promotion gate before claiming full parity.
- 2026-06-28: Follow-up staging closes two Round10 operator/user-visible tails without claiming live cutover: `codex_runtime::tests::run_codex_runtime_rejects_stdout_recovery_narration_without_final_answer` proves stdout recovery does not turn progress-panel narration into final `agent-reply` when no `final_answer` exists, and `status::tests::loop_status_prefers_fresh_loop_heartbeat_over_spawning_service_state` / `health::tests::healthz_prefers_fresh_loop_heartbeat_over_spawning_service_state` prove fresh alive loop heartbeats take precedence over stale/spawning Discord gateway service metadata in operator read models.
- 2026-06-28: Round10 progress-panel lane-cap heartbeat follow-up changed the cap semantics from both lanes to body/action lane only. `progress::tests::delivery_plan_status_heartbeat_after_body_cap_is_channel_agnostic` proves Telegram and Discord fixtures both keep status/current-step heartbeat edits after the body lane cap, include the capped/still-working marker plus latest internal event timestamp, and suppress identical heartbeat text after delivery. This is infrastructure behavior, not a Discord-only policy.
- 2026-06-28: Round10 live-cutover candidate follow-up adds immediate summary updates and generated-tool-output preview redaction. `progress::tests::delivery_plan_status_updates_immediately_for_new_current_step_after_body_cap` proves a new assistant execution summary/current step edits the status lane immediately even when the body/action lane is capped and the heartbeat interval has not elapsed. `codex_runtime::tests::codex_progress_redacts_artifact_payloads_from_tool_previews` proves generated image/tool output previews redact raw `data:image`, base64, provider URLs, and token-bearing strings before durable progress events.
- 2026-06-29: Discord attachment extraction follow-up adds `agent-harness-cli::tests::discord_gateway_text_attachment_becomes_bounded_artifact_metadata`, `agent-harness-cli::tests::discord_gateway_image_attachment_is_artifact_reference_without_payload`, `agent-harness-cli::tests::discord_gateway_rejects_non_discord_attachment_url_without_fetching`, `agent-harness-cli::tests::discord_gateway_attachment_download_failure_redacts_provider_url`, `agent-harness-cli::tests::discord_gateway_image_attachment_requires_supported_image_bytes`, `agent-harness-cli::tests::discord_gateway_json_like_attachment_uses_json_artifact_extension`, and `media::tests::discord_text_artifact_uri_resolves_and_passes_generic_safety`. These prove Discord `text/plain` attachments are downloaded into artifact storage with bounded text extraction summaries, Discord image attachments are artifact references without payload leakage, non-Discord attachment URLs fail closed before fetch, download-failure warnings do not carry raw provider URLs/tokens, declared image attachments require supported image bytes before storage, JSON-like attachments use `.json` artifacts, and `agent-harness://inbound-media/discord/...` resolves through the same harness-contained artifact safety path.
- 2026-06-29: Round10 Discord attachment artifact extraction live cutover passed fresh pre-cutover validation under `.debug\round10\discord-attachment-artifact-extraction-candidate-2026-06-29\validation-rerun-20260629`: `cargo fmt --all -- --check`, workspace check, full workspace tests, staged CLI build, `git diff --check`, topology explorer generation, candidate `invariants`, `schema-registry`, `release-checklist`, and public hygiene. Live cutover advanced canonical `target\debug\agent-harness.exe` to SHA-256 `90DEEDB7CD85EBCD2CA0FE8A04EE45D5BA2459683F0DC57C002CDEDA850F8391`, backed up the prior live binary as `target\debug\agent-harness.pre-round10-discord-attachment-extraction-20260629-013435.exe`, retained `target\debug\agent-harness.round10-discord-attachment-extraction-candidate-20260629-013435.exe`, and post-cutover recovery validation reported `healthz ready=true live=true readinessReady=true`, readiness `passed=59 warnings=1 failed=0`, worker/outbox/cron idle, `supervisor-reconcile` clean with eight running services, and `ops-cutover-receipt status=ready`. Live provider resend of a small Discord text attachment and image attachment remains the production-complete soak proof for the active CDN path.
- 2026-06-28: Round10 context-preflight/external-review closure added `runtime_pipeline::tests::run_runtime_queue_once_keeps_external_review_evidence_resumable_without_final_outbox`, proving Claude/second-brain review-only recovery evidence keeps the parent queue retryable/resumable and does not write a final outbox or completion receipt. Keep this gate with `codex_runtime::tests::run_codex_runtime_keeps_external_review_recovery_as_evidence_not_final` before promotion.
- 2026-06-28: Round10 artifact/context hygiene folded the image artifact note into the same candidate as generic artifact prompt hygiene. `media::tests::artifact_prompt_hygiene_renders_generic_bounded_summaries_not_payloads` covers image, audio/transcript, generated speech/media, browser capture, downloaded document, long tool-log/review transcript, worker report, and provider-native media classes. `runtime_worker::tests::prepare_runtime_queue_item_round_trips_inbound_media_artifacts_to_prompt` now proves the prepared main-thread prompt contains artifact refs and bounded extracted facts while redacting base64, provider URLs, Telegram `file_id`, and token-bearing source strings. Follow-up staging adds Codex progress preview redaction so generated media/tool previews do not reintroduce raw artifact payloads through progress receipts.
- 2026-06-28: Local Round10 gap-closure staging added focused prompt/memory boundary evidence. `memory::tests::non_main_memory_prompt_context_excludes_global_imported_snapshot_by_default` proves non-main recall excludes private global imported snapshot memory by default and exposes scope/filter telemetry. `prompt::tests::prompt_bundle_new_command_boundary_skips_prior_task_memory_context` proves a fresh `/new` session suppresses previous-task imported memory context during prompt assembly. This is local-only staging evidence; live cutover remains an operator handoff.
- 2026-06-28: Round10 adds initial `tool-use-timeout-recovery-gap` coverage. `codex_runtime::tests::run_codex_runtime_recovers_tool_use_idle_timeout_with_fresh_thread` proves a stalled `commandExecution` item records `toolUseTimeout`, stops the prior app-server/tool process, and continues through one fresh-thread recovery prompt. Full per-tool supervisor/cancellation parity remains future work.
- 2026-06-28: Topology gap labeling for progress delivery/final-surface regressions updated public docs and the machine-readable invariant/release-checklist catalog. Focused validation passed `cargo fmt --all -- --check`, `cargo test -p agent-harness-core quality_catalogs_and_hygiene_report_are_actionable --target-dir target\staging-progress-final-surface-gap -- --test-threads=1`, and public export hygiene with `forbiddenHits=[]`.
- 2026-06-28: Round10 full pre-cutover validation passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-round10-check`, full `agent-harness-core` tests (450 unit tests plus 5 integration tests and doc tests), full `agent-harness-cli` tests (54), image helper tests (15), staged build `target\staging-round10-build`, public export hygiene with `forbiddenHits=[]`, `invariants`, `schema-registry`, and `git diff --check`. Live cutover ticket `cutover-1782619189243` advanced canonical `target\debug\agent-harness.exe` to SHA-256 `229656F71806605650D5D7293F6B37F4362F511A18EBA386C22D06F5E45A4D2D`, backed up the previous binary as `target\debug\agent-harness.pre-round10-tool-timeout-progress-20260628-120311.exe`, restored all eight supervisor-reconcile-owned loops, and post-cutover validation reported `healthz ready=true live=true`, readiness `passed=59 warnings=1 failed=0`, worker/outbox idle, `supervisor-reconcile` clean, and `ops-cutover-receipt status=ready`.
- 2026-06-28: Follow-up live readback after Round10 cutover confirmed the same canonical SHA-256 `229656F71806605650D5D7293F6B37F4362F511A18EBA386C22D06F5E45A4D2D`, `healthz ready=true live=true readinessReady=true`, targeted Discord and Telegram outbox `pending=0 failed_retryable=0 invalid=0`, worker queue clear except the current interactive channel item, `supervisor-reconcile --all --dry-run` clean, and memory read-path `Ready` through the documented `migration-fallback`. It also recorded `supervisor-service-health-precedence-gap`: health/status can still expose stale `discord-gateway-loop` service metadata while the fresher loop heartbeat and reconcile inventory prove the service is running.
- 2026-06-12: `cargo fmt --all` passed.
- 2026-06-12: `cargo test -p agent-harness-cli --target-dir target\staging-test-cli` passed, 16 tests.
- 2026-06-12: `cargo test --workspace --target-dir target\staging-test-workspace` passed, 16 CLI tests, 207 core tests, 0 doc-tests.
- 2026-06-12: `cargo tree --workspace --duplicates` reported duplicate `webpki-roots` through `ureq`/TLS only.
- 2026-06-12: Provider-isolation hotfix added stale-guidance review to the release checklist and updated the builtin harness ops skill so OpenRouter routes use provider-specific Codex homes instead of the shared Codex/OAuth home.
- 2026-06-12: Ledger hygiene passed `cargo fmt --all`, `cargo test --workspace --target-dir target\staging-test-ledger-hygiene-workspace` (17 CLI tests, 210 core tests, 0 doc-tests), canonical `cargo build -p agent-harness-cli`, tracked-file public export `public-hygiene` with `forbiddenHits=[]`, live `jsonl-repair --apply` with backups for the three affected ledgers, live `healthz --target-home .\.agent-harness --require-writable-state`, and live `status --target-home .\.agent-harness --json` with affected ledger `invalidLines=0`.
- 2026-06-13: Response tone passed `cargo fmt --all`, `cargo test --workspace --target-dir target\staging-test-response-tone-workspace` (17 CLI tests, 215 core tests, 0 doc-tests), `git diff --check`, staging `cargo build -p agent-harness-cli --target-dir target\staging-build-response-tone`, tracked-file public export `public-hygiene` with `forbiddenHits=[]`, canonical `cargo build -p agent-harness-cli`, live `harness-skills-sync` for `agent-windows-harness` v0.1.4, live `healthz --target-home .\.agent-harness --require-writable-state`, and live `status --target-home .\.agent-harness --json`.
- 2026-06-13: Round4 reconnect/tone passed `cargo fmt`, `cargo test -p agent-harness-core` (219 tests), `cargo test -p agent-harness-cli` (17 tests), staging `cargo build -p agent-harness-cli --target-dir target\staging-build-round4-reconnect-tone`, `git diff --check`, tracked-file public export `public-hygiene` with `forbiddenHits=[]`, canonical `cargo build -p agent-harness-cli`, live `config-validate`, live `harness-skills-sync` for `agent-windows-harness` v0.1.5, live `healthz --target-home .\.agent-harness --require-writable-state`, and live `status --target-home .\.agent-harness --json`.
- 2026-06-13: Channel identity / delivery intent / cron scheduler pass completed `cargo fmt`, `cargo check`, `cargo test` (18 CLI tests, 229 core tests, 0 doc-tests), `git diff --check`, and tracked-file public export `public-hygiene` with `forbiddenHits=[]`; schema registry, README, operations handbook, feature parity Markdown/HTML, builtin skill, CLI help, and changelog were updated for the new contracts.
- 2026-06-14: Round4-2 live-control/native cron pass completed `cargo fmt --all --check`, `cargo check --workspace --target-dir target\staging-check-round4-2-live-guard`, `cargo test -p agent-harness-core --target-dir target\staging-test-round4-2-core` (239 tests), `cargo test -p agent-harness-cli --target-dir target\staging-test-round4-2-cli` (18 tests), `cargo build -p agent-harness-cli --target-dir target\staging-build-round4-2-live-guard`, public export hygiene with `forbiddenHits=[]`, and non-live cutover CLI smoke. Repo-root public hygiene is expected to fail because root intentionally contains live `.agent-harness` state.
- 2026-06-14: Round4-2 live cutover completed with ticket `cutover-1781376947099`; canonical build, supervisor-plan regeneration, bundled skill sync v0.1.9, direct runner start, `healthz ready=true`, and `status --json ready=true` passed with 7 live loop heartbeats and readiness `passed=59 warnings=0 failed=0`.
- 2026-06-15: Round5 cron/runtime isolation staged coverage passed for native cron CronRun admission/retry watermark recovery, dedicated cron worker lane, cron LLM worker runtime isolation, malformed cron worker recovery to retry-pending, worker/runtime CronRun control blockers, skipped runtime tombstones, namespaced sticky cron sessions, runtime class lock isolation, status cron/runtime summaries, and legacy root runtime lease compatibility. Verification passed `cargo fmt --all --check`, `cargo check --workspace --target-dir target\staging-check-round5-resume2`, full core/CLI/workspace tests, `cargo build -p agent-harness-cli --target-dir target\staging-build-round5-resume2`, `git diff --check` with CRLF warnings only, and public hygiene with `forbiddenHits=[]`. Live cutover completed with ticket `cutover-1781524146730`, backup label `pre-round5-cron-runtime-isolation-cutover`, canonical binary copied from `target\staging-build-round5-resume2\debug\agent-harness.exe`, bundled skill sync, regenerated 7-loop supervisor plan, direct runner start, `healthz ready=true live=true`, `status --json ready=true` with readiness `passed=59 warnings=0 failed=0`, worker `cron` lane limit `3`, runtime `cronQueuedItems=76`, `cronOpenItems=0`, and `cron-runs` `active=0 quarantined=0`.
- 2026-06-15: Source-home hotfix staged coverage passed `cargo fmt --all --check`, full CLI tests (23), targeted supervisor warning test, full core tests (257), `node --check tools\agent-discord-gateway\index.mjs`, `cargo check --workspace --target-dir target\staging-check-sourcehome`, `cargo build -p agent-harness-cli --target-dir target\staging-build-sourcehome`, `git diff --check` with CRLF warnings only, public hygiene with `forbiddenHits=[]`, and a non-live supervisor-plan smoke proving omitted `--source-home` writes channel/scheduler scripts with source-home equal to the target harness home.
- 2026-06-15: Source-home hotfix live cutover completed with ticket `cutover-1781537737517`, backup label `pre-sourcehome-routing-hotfix-cutover`, canonical binary copied from `target\staging-build-sourcehome\debug\agent-harness.exe`, previous binary backup `target\debug\agent-harness.pre-sourcehome-20260615233707.exe`, bundled skill sync v0.1.12, regenerated 7-loop supervisor plan with `.agent-harness` source-home, direct runner start, generated-script scan with no retired `.openclaw`/snapshot/container paths, `healthz ready=true live=true`, `status --json ready=true` with readiness `passed=59 warnings=0 failed=0`, `worker-status` pending/running/blockers `0`, `cron-runs` `active=0 quarantined=0`, registry resolving 24 agents, and Discord DM diagnostic `turn-plan` dispatching `AgentTurn` to `main`.
- 2026-06-16: Follow-up operator-surface pass completed `cargo fmt --all --check`, full core tests (259), full CLI tests (23), `cargo check --workspace --target-dir target\staging-check-followup`, `cargo build -p agent-harness-cli --target-dir target\staging-build-followup`, `git diff --check` with CRLF warnings only, public hygiene with `forbiddenHits=[]`, and staging smoke for `channel-outbox-plan --outbox-limit 100` plus `cron-scheduler-lint` `agentId`/`proposedAction` findings. Live cutover completed with ticket `cutover-1781543670554`, backup label `pre-followup-operator-surfaces-cutover`, previous binary backup `target\debug\agent-harness.pre-followup-operator-surfaces-20260616011809.exe`, bundled skill sync already current, regenerated 7-loop supervisor plan, direct runner start, `healthz ready=true live=true`, `status --json ready=true` with readiness `passed=59 warnings=0 failed=0`, and `worker-status` totals `pending=0 running=0 failedRetryable=0 failedTerminal=0`.
- 2026-06-16: Round6 context-window and same-session serialization staging passed `cargo fmt --all -- --check`, focused runtime worker/queue/pipeline/Codex/config/quality tests, full core tests (267), full CLI tests (23), `cargo check --workspace --target-dir target\staging-check-round6`, `cargo build -p agent-harness-cli --target-dir target\staging-build-round6`, staging `schema-registry` smoke for Codex context schemas, `git diff --check` with CRLF warnings only, and public hygiene with `forbiddenHits=[]`. The pass adds same-session interactive main-agent runtime serialization, Codex official compact preflight/retry/fallback recovery, `context-exhausted` runtime status, `codexContext` config validation/pass-through, and schema registry entries for `agent-harness.codex-context-preflight.v1`, `agent-harness.codex-context-checkpoint.v1`, and `agent-harness.codex-context-rollover.v1`. Live cutover completed with ticket `cutover-1781571095361`, backup label `pre-round6-context-session-serialization-cutover`, previous binary backup `target\debug\agent-harness.pre-round6-context-session-serialization-20260616085450.exe`, canonical binary copied from `target\staging-build-round6\debug\agent-harness.exe`, bundled skill sync, regenerated 7-loop supervisor plan, direct runner start, `healthz ready=true live=true`, `status --json ready=true` with readiness `passed=58 warnings=1 failed=0`, `worker-status` totals `pending=0 running=0 failedRetryable=0 failedTerminal=0`, and `cron-runs` `total=7 active=0 quarantined=0`.
- 2026-06-18: Round6-3 Telegram media, OpenClawMem adapter parity, and skill/context learning staging passed `cargo fmt --all --check`, `cargo check --workspace --target-dir target\staging-check-round6-3`, full core tests (323), full CLI tests (38), fixture-backed Telegram media smoke, staging memory smoke, `git diff --check` with line-ending warnings only, and public export hygiene with `forbiddenHits=[]`. Live cutover completed with ticket `cutover-1781718103242`, backup label `pre-round6-3-skill-lifecycle-cutover-prestop`, previous binary backup `target\debug\agent-harness.pre-round6-3-skill-lifecycle-20260618014423.exe`, canonical binary copied from `target\staging-build-round6-3\debug\agent-harness.exe`, preserved 8-loop direct-runner topology including `telegram-loop-xiaoxiaoli`, bundled skill sync skipped the user-modified live builtin without `--force`, `healthz ready=true live=true`, `enable-check` and `status --json` readiness `passed=59 warnings=0 failed=0`, `worker-status` totals `pending=0 leased=0 running=0 failedRetryable=0 failedTerminal=0`, `memory-service-status status=ready`, and `memory-read-path-smoke status=ready`.
- 2026-06-20: Round8 progress/tool-use indicator regression fix staged validation passed `cargo fmt --all --check`, full core tests (332), full CLI tests (38), `cargo check --workspace --target-dir target\staging-check-round8-progress-health`, staged build `target\staging-build-round8-progress-health`, and public export hygiene with `forbiddenHits=[]`. Live cutover completed with ticket `cutover-1781943253495`, backup label `pre-round8-progress-liveness-cutover-prestop`, previous binary backup `target\debug\agent-harness.pre-round8-progress-liveness-20260620161705.exe`, initial versioned live binary `target\debug\agent-harness.round8-progress-liveness.exe` while canonical `target\debug\agent-harness.exe` was locked, and follow-up canonical repair by renaming the locked prior binary to `target\debug\agent-harness.locked-canonical-before-round8-20260620210500.exe`, restoring canonical to the Round8 SHA-256 `EFBBB9F42652C34CDCB0BDABB9FC4E401673CED3A4A038E29724938253A6E8F7`, and switching live scripts back to canonical with no remaining versioned refs. Final live validation preserved the 8-loop direct-runner topology, `healthz ready=true live=true`, `enable-check` readiness `passed=59 warnings=1 failed=0` with `progress-delivery-loop` live, `worker-status` totals `pending=0 leased=0 running=0 failedRetryable=0 failedTerminal=3`, `memory-service-status status=ready`, `memory-read-path-smoke status=ready`, and backup inventory recorded at `.debug\round8\round8-backup-inventory.md`.
- 2026-06-21: Round8 gateway stability staged validation passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-check-round8-gateway-stability`, full core tests (339), full CLI tests (39), `cargo build -p agent-harness-cli --target-dir target\staging-build-round8-gateway-stability`, `git diff --check` with line-ending warnings only, public export hygiene with `forbiddenHits=[]`, and a public-facing best-effort string scan whose hits were expected public docs placeholders or ignored public-state names. The pass added dead legacy PID runtime lease reaping with `stale-owner-reaped` receipts, durable `lease-acquired` receipts, atomic loop heartbeat writes, corrupt heartbeat status/health fields, progress-delivery ledger cursor/cache compaction, progress-loop health isolation, structured JSON supervisor stop files, no-`Tee-Object` generated runners, bundled `agent-windows-harness` skill v0.1.13, schema registry entries for progress delivery state and supervisor stop files, changelog notes for `v0.1.1`, and pre-cutover live baseline evidence showing the old live gateway unhealthy before the approved cutover.
- 2026-06-21: Round8 gateway stability live cutover completed with ticket `cutover-1782046248578`, backup label `pre-round8-gateway-stability-cutover-prestop`, previous-binary backup `target\debug\agent-harness.pre-round8-gateway-stability-20260621205307.exe`, canonical live SHA-256 `55692DD0670E538CB0EE099F2F576FD3606CFB7F31FC696325E020F32915EB57`, preserved 8-loop topology, no remaining `Tee-Object` references in preserved live scripts, forced live builtin skill sync to `agent-windows-harness` v0.1.13, xiaoxiaoli offset repair from the last valid retained backup, `healthz ready=true live=true` with 8 fresh non-corrupt loops, `enable-check Ready: yes` with `passed=58 warnings=2 failed=0`, `worker-status pending=0 leased=0 running=0 failedRetryable=0 failedTerminal=3`, `memory-service-status Status: Ready`, `memory-read-path-smoke Status: Ready`, stop files clear, GitHub release `v0.1.1` published, and cutover receipt appended with `status=ready` / `passed=59 warnings=1 failed=0`.
- 2026-06-21: Round8 runner backoff follow-up passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-check-round8-runner-backoff`, focused supervisor script generation test, focused quality/schema registry test, and public export hygiene with `forbiddenHits=[]`. The follow-up classifies generated runtime-runner process-level exits as `process-exit` or `resource-exhausted`, records `errorClass` in `runtime-loop-runner-safe-mode.json`, and uses a longer bounded restart delay for OOM/memory-pressure signatures.
- 2026-06-21: Round8 observe-only supervisor registry follow-up passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-check-round8-supervisor-registry`, full core tests (341), full CLI tests (39), `cargo build -p agent-harness-cli --target-dir target\staging-build-round8-supervisor-registry`, public export hygiene with `forbiddenHits=[]`, and changed public/operator docs path hygiene with `forbiddenHits=[]`. The follow-up adds loop `generationId` metadata, per-service `agent-harness.supervisor-service-state.v1` records under `state\supervisor\services`, `status --json` `loops.services`, and `healthz` `supervisorServices` without changing external runner ownership.
- 2026-06-21: Round8 memory-pressure gate follow-up passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-check-round8-memory-gate`, full core tests (341), `cargo build -p agent-harness-cli --target-dir target\staging-build-round8-memory-gate`, public export hygiene with `forbiddenHits=[]`, and changed operator docs/skill path hygiene with `forbiddenHits=[]`. Generated runtime-loop runners now write a temporary structured stop file for `progress-delivery-loop` and record `memoryGateDecision` before restarting after OOM/resource-exhaustion signatures.
- 2026-06-21: Round8 supervisor-owned progress follow-up passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-check-round8-supervisor-progress`, full core tests (341), full CLI tests (40), `cargo build -p agent-harness-cli --target-dir target\staging-build-round8-supervisor-progress`, `git diff --check`, focused supervisor-run/status/health/schema coverage, public export hygiene with `forbiddenHits=[]`, and changed public/operator docs path hygiene with `forbiddenHits=[]`. The follow-up adds `supervisor-run --service progress-delivery-loop`, generated progress runner handoff to that Rust supervisor wrapper, supervised child generation metadata, and service-state restart/backoff fields.
- 2026-06-21: Round8 supervisor-owned final outbox follow-up passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-check-round8-supervisor-outbox`, full core tests (341), full CLI tests (41), `cargo build -p agent-harness-cli --target-dir target\staging-build-round8-supervisor-outbox`, `git diff --check`, focused supervisor-run final-outbox/status/health/schema coverage, public export hygiene with `forbiddenHits=[]`, changed public/operator docs path hygiene with `forbiddenHits=[]`, and public-facing content scan (`files=156`, `patterns=8`). The follow-up adds `supervisor-run --service discord-outbox-loop`, generated Discord outbox runner handoff to that Rust supervisor wrapper, final-delivery priority metadata, and a shorter restart backoff for final Discord delivery.
- 2026-06-21: Round8 runtime lease owner envelope follow-up passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-check-round8-lease-owner`, focused `runtime_worker::tests`, full core tests (342), full CLI tests (41), `cargo build -p agent-harness-cli --target-dir target\staging-build-round8-lease-owner`, `git diff --check`, public export hygiene with `forbiddenHits=[]`, and added-line public/operator docs path hygiene with `forbiddenHits=[]`. The follow-up writes structured runtime queue lease owner envelopes with service id, generation id, PID, process start time, and acquisition time while preserving legacy `owner="pid:<n>"` parsing and stale-owner reaping.
- 2026-06-21: Round8 runtime generation reconciliation and channel `/restart` follow-up passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-check-round8-restart-clean`, full core tests (344), full CLI tests (41), `cargo build -p agent-harness-cli --target-dir target\staging-build-round8-restart`, `git diff --check`, and public export hygiene excluding `.debug` with `forbiddenHits=[]`. The follow-up adds `runtime-lease-reconcile`, generated runtime child-generation reconciliation, admin-only channel `/restart`, `agent-harness.channel-restart-request.v1`, `agent-harness.runtime-queue-lease-reconciliation.v1`, and restart-aware generated channel runners.
- 2026-06-22: v0.1.2 live cutover completed with ticket `cutover-1782069897799`, backup label `pre-v0.1.2-cutover-prestop`, previous-binary backup `target\debug\agent-harness.pre-v0.1.2-20260622032602.exe`, canonical live SHA-256 `B8A32018154FB62561B464BF71C3949A509352ED5276EE78DEED9A2F97D8C667`, preserved 8-loop direct-runner topology under the shared supervisor plan/heartbeat/stop-file control plane, forced live builtin skill sync to `agent-windows-harness` v0.1.20, `healthz ready=true live=true` with 8 fresh non-corrupt loops and no stop files, `status --json` and `enable-check` readiness `passed=59 warnings=1 failed=0`, `worker-status pending=0 leased=0 running=0 failedRetryable=0 failedTerminal=3`, `memory-service-status Status: Ready`, `memory-read-path-smoke Status: Ready`, and latest cutover receipt appended with `status=ready` / `passed=59 warnings=1 failed=0`.
- 2026-06-23: v0.1.3 mem-engine layer-owner bridge staged validation passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-check-mem-engine-canary-status`, full core tests (347 unit tests, 5 integration tests, 0 doc-tests), full CLI tests (41), `cargo build -p agent-harness-cli --target-dir target\staging-build-mem-engine-canary-status`, public export hygiene with `forbiddenHits=[]`, and the promoted-owner canary regression test. The pass routes active `mem-engine` recall through the `openclaw-mem-engine` bridge envelope, reports provider/backend/fallback/bridge/write/policy telemetry across CLI, status, MCP, and read-path smoke, keeps unreachable bridge fallback read-only, and fixes stale canary wording so promoted live owners report `mem-engine-active`.
- 2026-06-23: v0.1.3 live cutover completed in two protected steps. Bridge cutover ticket `cutover-1782175446310` used backup label `pre-mem-engine-bridge-cutover-prestop-20260623084405`, previous SHA `B8A32018154FB62561B464BF71C3949A509352ED5276EE78DEED9A2F97D8C667`, and interim SHA `A3B8212E714780BB207BC6FBB4BD8AA24BAEBFA87B41BB232EC01F040D5C6E22`. Canary-status follow-up ticket `cutover-1782177709754` used successful pre-stop backup label `diagnostic-mem-engine-canary-prebackup-20260623092318`, previous-binary backup `target\debug\agent-harness.pre-mem-engine-canary-20260623092447.exe`, and final canonical SHA-256 `34B874745E0B10A9BAC7D0685929788C227BBFB0B5E5087507E3BC0D7CC1AAC8`. Final live validation preserved 8 loops, `healthz ready=true live=true`, `enable-check passed=58 warnings=2 failed=0`, `worker-status pending=0 leased=0 running=0 failedRetryable=0 runtimeOpenItems=0 activeCronRuns=0`, `memory-service-status Status: Ready Mode: local-in-process Active slot owner: mem-engine Mem-engine canary: mem-engine-active`, and `memory-read-path-smoke Status: Ready Recall: Ready writesPerformed=no` with explicit `migration-fallback` because the live bridge response file is absent.
- 2026-06-23: Progress delivery mute staged validation passed `cargo fmt --all -- --check`, focused core `trace`, `progress_delivery`, and `config` tests, focused CLI `progress_delivery` tests, `cargo check -p agent-harness-cli --target-dir target\staging-check-progress-mute`, full workspace tests under `target\staging-test-progress-mute-workspace` (41 CLI tests, 351 core tests, 5 integration tests, 0 doc-tests), `cargo build -p agent-harness-cli --target-dir target\staging-build-progress-mute`, `git diff --check`, public export hygiene with `forbiddenHits=[]`, schema registry, invariants, and completed/canceled/timeout/dead-letter trace samples on a temporary staging fixture. The guarded live cutover used ticket `cutover-1782197836816`, backup label `pre-progress-mute-cutover-prestop-20260623150004`, previous-binary backup `target\debug\agent-harness.pre-progress-mute-20260623150228.exe`, canonical SHA-256 `B5747289D81675AC21CE6594FA3A2A8CE1DAA7419A1CD5D3DE1E89F6A8357475`, and live config backup `.agent-harness\harness-config.json.pre-progress-mute-20260623150354.bak`. Post-cutover validation preserved 8 loops, `healthz ready=true live=true`, `enable-check passed=59 warnings=1 failed=0`, `worker-status pending=0 leased=0 running=0 failedRetryable=0 failedTerminal=4 runtimeOpenItems=0 activeCronRuns=0`, `config-validate status=valid`, `ops-cutover-receipt status=ready`, and fixture-backed progress smoke with `Muted events=1`, `Sent panels=0`, `Edited panels=0`, `Failed deliveries=0` for Xiaoxiaoli group `telegram:-1003968507595`.
- 2026-06-23: Codex CLI log-churn dependency live maintenance completed. The local `.tools\codex-cli` install was upgraded from `codex-cli 0.137.0` to `codex-cli 0.142.0` after an idle gate with `workerPending=0`, `workerLeased=0`, `workerRunning=0`, `workerFailedRetryable=0`, `runtimeOpenItems=0`, and `activeCronRuns=0`. Rollback artifacts were saved under `.debug\codex-upgrade-1\backup-20260623-215003`; harness backup label `pre-codex-cli-log-churn-upgrade-20260623-215003` copied `6568` files with `30` skipped and no warnings. Scoped harness `logs_2.sqlite*` files were cleaned while local app-server processes were stopped, global Codex Desktop logs were left untouched because the desktop app-server was active, the 8-loop direct-runner topology was restarted, `codex-launch-probe` recorded `status=started-and-stopped`, 60-second harness log sample showed `DeltaKB=0`, and post-cutover validation reported `healthz ready=true live=true`, `enable-check/status passed=59 warnings=1 failed=0`, `worker-status pending=0 leased=0 running=0 failedRetryable=0 runtimeOpenItems=0 activeCronRuns=0`, `config-validate status=valid`, and `ops-cutover-receipt status=ready`.
- 2026-06-25: Codex runtime executable pinning and progress-delivery state pruning passed TDD red/green coverage for supervisor executable discovery, Windows Codex preflight rejection of extensionless/MSIX shims, and delivered-terminal progress cache pruning. Focused validation passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-check`, `cargo test -p agent-harness-cli --target-dir target\staging-cli-tests -- --test-threads=1` (47 tests), focused `agent-harness-core` supervisor/progress/preflight/runtime tests, `cargo test -p agent-harness-core --target-dir target\staging-full-core -- --test-threads=1 --skip codex_runtime::tests::run_codex_runtime` (380 unit tests plus 5 integration tests; long app-server runtime tests were run only as focused probes because the full unskipped set exceeded the 10-minute cutover validation budget), `cargo build -p agent-harness-cli --target-dir target\staging-cutover-build`, `git diff --check` with CRLF warnings only, and public export hygiene with `forbiddenHits=[]`. Live cutover ticket `cutover-1782379567476` used stopped-state backup label `pre-codex-runtime-pin-progress-prune-cutover-stopped-20260625`, previous-binary backup `target\debug\agent-harness.pre-codex-runtime-pin-progress-prune-20260625-173028.exe`, and canonical SHA-256 `E81409D0093962792ECDF27BFBBA8F847426775E1D65F57089805B5E62743CDE`. Supervisor scripts now pin `--codex-exe` to the repo-local vendor `codex.exe`; post-cutover `codex-launch-probe` recorded `status=started-and-stopped`, `healthz ready=true live=true`, `enable-check/status passed=59 warnings=1 failed=0`, `worker-status pending=0 leased=0 running=0 failedRetryable=0 runtimeOpenItems=0`, Telegram and Discord outbox plans reported `pending=0`, progress state pruned to `queues=0 compactedEvents=0`, and `ops-cutover-receipt status=ready`.
- 2026-06-26: Round9 integrated workflow passed final staged validation with `cargo fmt --all -- --check`, workspace check, `agent-harness-core` tests (429 unit tests plus 5 integration tests and doc tests), `agent-harness-cli` tests (53), image helper tests (10), sparse-runner tests (5), staged build, `git diff --check` with CRLF warnings only, and public export hygiene with `forbiddenHits=[]`. Live cutover ticket `cutover-1782427345146` used backup label `pre-round9-integrated-cutover-20260626-064059`, previous-binary backup `target\debug\agent-harness.pre-round9-integrated-20260626-064224.exe`, canonical SHA-256 `D93A38F1677AB9D32879D7A4BB1F128F56F7124467CF1D3A384B4B6B2453CAA1`, recovery/restart tickets `cutover-1782427493136`, `cutover-1782427558106`, and `cutover-1782428072775`, and post-cutover `healthz ready=true live=true`, `enable-check/status passed=58 warnings=2 failed=0`, `worker-status pending=0 leased=0 running=0 failedRetryable=0 failedTerminal=5`, `ops-cutover-receipt status=ready`, and live Codex image route probe `.debug\round9\live-image-route-probe-20260626-070026.json` with `ok=true`, `routeStatus.available=true`, `proof=generated-image-artifact`, and `directApiFallback=false`.
- 2026-06-26: Round9-1 lifecycle and image-timeout follow-up passed fresh cutover validation with `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-round9-1-cutover-check`, `agent-harness-core` tests (431 unit tests plus 5 integration tests and doc tests), `agent-harness-cli` tests (53), image helper tests (14), sparse-runner tests (6), staged build `target\staging-round9-1-cutover-build`, public export hygiene with `forbiddenHits=[]`, and `git diff --check`. Source commit `628fe36` was already pushed to `origin/codex/round9-integrated-implementation`; live cutover ticket `cutover-1782447196301` used stopped-state backup label `pre-round9-1-followup-cutover-stopped-20260626-122051`, previous-binary backup `target\debug\agent-harness.pre-round9-1-followup-20260626-122051.exe`, previous SHA-256 `D93A38F1677AB9D32879D7A4BB1F128F56F7124467CF1D3A384B4B6B2453CAA1`, and canonical SHA-256 `946D0AC01F6DF266D2D356A5A8342B3D87238E9E7D56C8C653CDEC079BACA908`. Post-cutover validation reported `healthz ready=true live=true readinessReady=true`, 8 supervisor-reconcile-owned services, `telegram-loop-xiaoxiaoli` supervisor and child command lines with `--agent xiaoxiaoli`, `status passed=58 warnings=2 failed=0`, `worker-status pending=0 leased=0 running=0 failedRetryable=0 failedTerminal=5 runtimeOpenItems=0 activeCronRuns=0`, and `ops-cutover-receipt status=ready`.
- 2026-06-26: Round9-1 session/compact/live-cutover preparation passed consolidated root-cause validation for Codex compact empty-final handling, Xiaoxiaoli/non-main DM session isolation, OperationPlan scope isolation, lifecycle `authVisibility=verified`, and repo-local Codex CLI `0.142.2` stable. Fresh staging validation passed `cargo fmt --all -- --check`, `cargo check --workspace --target-dir target\staging-subagent-lifecycle-cutover-check`, `agent-harness-core` tests (437 unit tests plus 5 integration tests and doc tests), `agent-harness-cli` tests (53), image helper tests (14), sparse-runner tests (6), Round9-1 lifecycle matrix receipt `.debug\round9-1\subagent-lifecycle\receipts\20260626-195937-matrix.json` with `allOk=true`, staged build `target\staging-subagent-lifecycle-live-build` SHA-256 `84D0A2418127449B089C8D0158A1B4AC6A0DB8280FCB5A473BD3D5C4551E1F2D`, public export hygiene with `forbiddenHits=[]`, and `git diff --check` with CRLF warnings only. This was the pre-cutover gate before explicit operator approval for the sandbox-mode patch and restart.
- 2026-06-26: Round9-1 session/compact live cutover completed with ticket `cutover-1782477023513`. The live config backup is `.debug\round9-1\subagent-lifecycle\cutover-backups\20260626-202742\harness-config.json.pre-round9-1-session-compact-codex-0.142.2-cutover`; the live security block now uses `codexApprovalPolicy=accept`, `codexSandboxMode=disabled`, and `codexSandboxPolicy=dangerFullAccess`, with legacy `codexSandbox` removed. The previous live binary was backed up as `target\debug\agent-harness.pre-round9-1-session-compact-20260626-203023.exe` with SHA-256 `946D0AC01F6DF266D2D356A5A8342B3D87238E9E7D56C8C653CDEC079BACA908`; canonical `target\debug\agent-harness.exe` is now SHA-256 `84D0A2418127449B089C8D0158A1B4AC6A0DB8280FCB5A473BD3D5C4551E1F2D`. Post-cutover validation reported `healthz ready=true live=true readinessReady=true` with 8 non-stale loop heartbeats, scoped supervisor repair for `telegram-loop-xiaoxiaoli` with `--agent xiaoxiaoli` and `--telegram-account xiaoxiaoli`, generated default/OpenRouter Codex configs with no `[windows]` or `sandbox =` entries, sandbox log tail with no new post-cutover setup refresh, `status passed=58 warnings=2 failed=0`, `worker-status pending=0 leased=0 running=0 failedRetryable=0 failedTerminal=5 runtimeOpenItems=0 activeCronRuns=0`, post-cutover lifecycle matrix `.debug\round9-1\subagent-lifecycle\receipts\20260626-204739-matrix.json` with `allOk=true`, and `ops-cutover-receipt status=ready`.
