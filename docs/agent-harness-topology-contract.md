# Agent Harness Topology Contract

Date: 2026-07-15

This contract turns the architecture handoff into release-gate input. Use it before changing channel ingress, channel state, prompt assembly, runtime queueing, Codex execution, final outbox, delivery, progress, workers, cron, or memory. The operations handbook remains the live operational source of truth; this file is the compact dependency map and scenario matrix that reviewers and external coding agents should apply to code changes.

## Identity Axes

Every channel-origin turn is keyed by these axes. A change that drops, normalizes, or compares only a subset of them can create cross-agent or cross-session regressions.

| Axis | Meaning | Must Be Preserved Through |
|---|---|---|
| `platform` | Telegram, Discord, or another channel adapter. | ingress, queue, progress, outbox, delivery receipts, trace |
| `accountId` | Provider account or bot identity when configured. | channel identity, outbox, delivery, receipts |
| `channelId` | DM/chat/channel/thread address. | channel state, queue item, delivery intent, trace |
| `userId` | Sender identity inside the channel. | permission gate, channel state, session key, trace |
| `agentId` | Runtime owner, not just persona text. | provider/model defaults, Codex home, prompt files, memory namespace, runtime lane, session freshness, outbox, delivery, OperationPlan scope |
| `sessionKey` | Conversation/session continuity key. | command state, runtime queue, Codex plan, transcript, freshness guard, prompt/memory task boundary |
| `virtualSessionId` | Stable task-continuity owner across concrete-session rollover. | working-context resolver, prompt manifest, operation plan, child result owner, coordinator resume |
| `runtimeClass` | Interactive, cron, worker, maintenance, or another runtime lane. | queue selection, lease, retry/dead-letter policy, status |
| `sourceQueueId` | Correlation for one queued turn. | Codex completion, final outbox, delivery receipts, trace |
| `backendContextGeneration` | Opaque Codex/backend context generation, not a routing wildcard. | prompt manifest, injection ledger, reinjection decision, prepared execution receipt |

## Ownership Boundaries

| Component | Owns | Reads | Writes | Required Invariants |
|---|---|---|---|---|
| `channel_identity` | fail-closed channel/account/agent binding | config identity bindings | identity-check receipts | I1, I8 |
| `channel_state` | slash-command state and active session metadata; `/think` and `/reasoning` are one control surface | channel command input | `state/channels/<platform>/<channel>/<user>/state.json` | I4, I8 |
| `channel_ingress` / `channel_runtime` | command-vs-agent-turn decision | inbound platform event, identity binding, channel state | runtime queue item or command outbox | I1, I8 |
| `runtime_queue` / `runtime_worker` | durable queue item lifecycle and lane concurrency | pending queue, leases, runtime class config | pending/status/lease receipts | I1, I3, I5, I6, I8 |
| `media` / artifact hygiene | harness-contained artifact metadata, extraction summaries, prompt-safe artifact refs, outbound media policy decisions | inbound attachments, generated media/tool receipts, artifact metadata, outbound local paths and harness artifact refs | `agent-harness.inbound-media-artifact.v1`, `agent-harness.outbound-media-policy.v1`, prompt-safe artifact summaries | I8, I11, I16 |
| `prompt` / `turns` | exact-full-lane, agent-scoped prompt bundle, eight-file OpenClaw-style per-agent config manifest, agent-scoped skill source/usage-prior selection, and backend-generation reinjection | registry, selected agent home/workspace and shared allowlisted skill namespaces, agent workspace/config files including static `MEMORY.md`, dynamic per-agent memory recall, channel state, artifact summaries, full lane, backend context generation | prompt bundle, prompt manifest, skill-selection receipt, and lane-scoped injection ledger | I8, I11, I20 |
| `workers` / child execution policy | immutable per-child provider/model/reasoning/tool/sandbox/budget snapshot, pre-lease exhausted-job terminalization, and bounded execution/process-tree cancellation | parent operation plan, exact owner, child admission policy | worker job, immutable policy snapshot, lifecycle/terminal receipts | I3, I4, I7, I8 |
| `cron_scheduler` / `deterministic_cron` | exact crontab source discovery, timezone/calendar evaluation, per-entry execution policy, occurrence identity, watermarks, and catch-up authority | canonical cron entries, exact `.crontab` sources, scheduler config, prior tick evidence | CronRun/worker admission, occurrence metadata, job decisions, watermarks, tick receipts | I3, I5, I7, I17 |
| worker result mailbox / coordinator resume | exact-owner child result collection and master-only durable continuation | typed terminal child receipts, bounded/redacted result summaries or opaque artifact pointers, exact-lane activity | mailbox claims, explicit failed omissions, resume intent, coordinator wait/consume receipts, structural-only master continuation context | I2, I3, I8, I9 |
| `codex_runtime` | app-server plan/executable resolution, preflight, startup/run, completion capture, tool-timeout recovery, and interrupted-command evidence | prompt bundle, Codex config, provider request policy | execution receipts, terminal protocol-error receipts, transcripts, completions, `toolUseTimeout` summaries, `interruptedToolUses` summaries | I2, I3, I7, I10, I19 |
| `provider_request_policy` | provider/model capability gates and optional per-request acceleration overrides such as priority/fast mode | agent/session command state, provider route, model id, provider profile | request-plan overrides, command receipts, unsupported/skip reasons | I7, I8 |
| `runtime_pipeline` | run-once orchestration, retry/dead-letter, final outbox repair, interruption summary classification, outbound media directive parsing and artifactRef resolution | queue item, Codex receipts, channel state, media policy config | run-once receipt, dead-letter receipt, final outbox, media policy/lint receipts | I2, I3, I7, I8, I9, I16, I19 |
| `channel_delivery` | provider-native delivery, retry, outbound presentation rendering, text chunking, terminal receipt precedence, and attachment-kind-aware media upload | outbox JSONL, optional rich-message presentation payloads, policy-validated attachments | delivery receipts, per-provider rendered-unit receipts with attachment kind and per-chunk text ids | I2, I7, I9, I14, I16, I18 |
| `progress` | source-authoritative progress narration delivery, separate from final replies | progress events/index, channel policy, watched stop file | progress outbox/delivery state, source cursor/cache, surface claims | I2, I4, I9, I17, I25, I27 |
| `ledger_maintenance` | source-aware, expensive runtime/channel-delivery/progress history retention outside interactive paths | coalesced terminal wakes, committed ledger/index state, maintenance policy | bounded per-ledger maintenance report and isolated warnings | I3, I5, I9, I25 |
| `supervisor` plan/reconcile | derive durable Windows Task Scheduler and reconcile ownership from the authoritative harness supervisor config | enabled `supervisor.telegramLoops`, loop/service identities, maintenance-owner selection | generated start/stop scripts, scheduled-task plan receipt, reconcile inventory/launch plan | I26 |
| `memory` / session-history retrieval | recall/lifecycle/store proposal namespaces and lane-bound session-history lookup | prompt bundle, memory config, pack artifact metadata | memory receipts, graph freshness receipts, pack retrieval receipts | I8, I15 |

## Mandatory Impact Matrix

Use this table whenever a diff touches one of the listed areas. The listed docs and tests are minimums; add narrower tests for the behavior being changed.

| Changed Area | Review These Public Docs | Minimum Scenario Pack |
|---|---|---|
| Channel identity, commands, or state | `docs/trust-boundaries.md`, `docs/configuration.md`, `docs/invariants.md`, this file | allow/deny and ambiguous-binding gates; non-main routing; `/new`, `/model`, unified `/think`/`/reasoning`, `/stop`; agent-scoped state and fresh-task boundaries |
| Runtime queue, worker mailbox, coordinator, or leases | `docs/agent-worker-dispatch-strategy.md`, `docs/configuration.md`, `docs/invariants.md`, `docs/schema-registry.md`, this file | FIFO/class isolation; `WorkerJobKind`-derived route; typed terminal provenance; immutable heterogeneous policies; exact-owner mailbox; failed omissions; structural-only context; exact-lane mutual exclusion; idempotent replay; master-only user output |
| Codex runtime, final outbox, delivery, or progress | `docs/invariants.md`, `docs/schema-registry.md`, this file | completion/protocol-error terminality; duplicate and stale-owner suppression; progress/final separation; provider delivery and terminal receipt precedence; source-index freshness/defer; historical fresh-send suppression; watched-stop claim release; bounded interruption evidence |
| Presentation, media, or artifact hygiene | `docs/invariants.md`, `docs/schema-registry.md`, this file | protected parsing; policy-gated artifact refs; bounded extracted context; provider escaping/batching; no raw payloads, credentials, provider URLs, or local paths in durable model-facing context |
| Prompt assembly or agent selection | `docs/configuration.md`, `docs/invariants.md`, this file | absent non-main directory isolation; safe worker agent ids and canonical session-path confinement; exact-lane/backend-generation manifest; eight static files and aliases; tombstones; dynamic-memory separation; `/new` isolation |
| Provider/model/request overrides | `docs/configuration.md`, `docs/invariants.md`, `docs/migration-0.8.md`, this file | exact-route `max`; exact `ultra` rejection; legacy alias canonicalization; unsupported routes fail closed; command replies remain model-turn-free |
| Memory or session-history search | `docs/invariants.md`, this file | per-agent namespace; bounded provenance; no canonical writes through read caches; exact-lane session history; explicit project/global scope |
| Release or rollback boundary | `docs/configuration.md`, `docs/invariants.md`, `docs/migration-0.8.md`, this file | candidate gates, idle boundary, no `ultra` enablement, source/V2 compatibility review, safe state rollback, and post-change health checks |
| Supervisor, Task Scheduler, or cutover ownership | `docs/invariants.md`, `docs/configuration.md`, this file | configured loop plan/reconcile parity; generated start/stop bundle preserves enabled channel loops; dedicated maintenance owner and fresh post-cutover heartbeats |

## Agent-Boundary Scenario Pack

Run or add tests equivalent to these scenarios for channel/runtime/session changes:

1. Same agent, same channel/user, old session completes after `/new`: final reply is suppressed and an explicit stale-session warning is recorded.
2. Different agents, same platform/channel/user, one agent has an active session in shared channel state: the other agent's completed turn is not suppressed solely because the shared state belongs to a different `agentId`.
3. Non-main agent normal message reaches prompt assembly with that `agentId`, its own workspace/prompt files, provider/model defaults, Codex home, and memory namespace.
4. Completed turn produces exactly one source-correlated final outbox record or one terminal error/dead-letter notification.
5. Trace reconstruction can connect ingress, queue id, Codex completion, final outbox, and delivery or terminal notification.
6. Fresh request after `/new` uses the new concrete session as a task boundary: previous-task imported memory context is not injected unless a later policy explicitly opts in.
7. `/think max` and `/reasoning max` are command-only aliases over one agent-scoped state; the next normal turn carries exact `max`, while a sibling agent sharing platform/account/channel/user does not inherit it. Exact `ultra` is rejected and `ultra-high` remains the legacy `xhigh` alias.
8. Prompt manifest and injection ledger bind to the exact full lane plus backend context generation. A sibling agent or different lane cannot reuse another agent's injected configuration, and a changed/deleted config file or changed backend generation produces the expected bounded reinjection/tombstone behavior.
9. Sibling children may use different immutable provider/model/effort policies. Retry cannot silently change a child's policy, and every expected terminal child contributes either a bounded/redacted result with opaque pointers or an explicit failed-omission envelope to the exact parent owner.
10. Child/external-agent progress, completion, and error are internal evidence. The exact master coordinator consumes only validated envelope fields and structural metadata, waits until the whole exact lane is idle, schedules one idempotent continuation, and is the only participant allowed to emit parent user-facing output.

The regression test `runtime_pipeline::tests::channel_session_freshness_does_not_cross_suppress_other_agent` covers scenario 2. The regression test `prompt::tests::prompt_bundle_new_command_boundary_skips_prior_task_memory_context` covers scenario 6 for prompt assembly.

## Support-Plane Knowledge Graphs

The topology contract is canonical text. Generated graph tools are support-plane aids: they help reviewers discover relationships, but they do not replace source code, tests, release gates, or openclaw-mem Store/Pack/Observe ownership.

Understand-Anything is a good fit for codebase topology exploration because it can generate `.understand-anything/knowledge-graph.json` with file/function/class/dependency nodes, architecture layers, guided tours, semantic search, and diff-impact views. Use it for these bounded jobs:

- Build or refresh a repo-local code topology graph after behavior-changing architecture work.
- Ask code-graph questions such as "which modules touch session freshness?" or "what changes affect final outbox delivery?"
- Compare the generated graph against this contract to find missing component edges or stale docs.
- Produce reviewer-facing navigation aids for large changes before release review.

Do not use it for these jobs:

- Do not treat generated graph output as the canonical memory store.
- Do not let it write live harness state, secrets, channel ledgers, or `.agent-harness` private data into tracked documents.
- Do not enable auto-update or dashboard launch as a release gate until the graph artifacts, ignore policy, and public hygiene behavior are verified.
- Do not merge generated graph facts into openclaw-mem as trusted facts without provenance, freshness, and human-readable source file pointers.

Recommended staged adoption:

1. Create `.understand-anything/.understandignore` that excludes `.agent-harness/`, `.debug/`, `.tmp/`, `target/`, `.public-export/`, secrets, generated images, and large ledgers.
2. Run a scoped graph build for tracked source and docs only.
3. Save the graph under `.understand-anything/knowledge-graph.json` as ignored local support state unless a separate public export policy is approved.
4. Record a compact memory/graph freshness receipt that links the graph commit hash, topology contract version, and source docs used for generation.
5. Use the graph to support impact review, while keeping `docs/agent-harness-topology-contract.md`, `docs/invariants.md`, and tests as the release gates.

`docs/topology-explorer.html` is the lightweight human-facing companion view for this contract. Generate it from the public topology source by running:

```powershell
python tools\generate_topology_explorer.py
```

The generated page embeds `docs/topology-explorer-data.json` so it can be opened directly from disk. It may help operators browse components, identity axes, release gates, and open gaps, but it is still a support-plane artifact; do not edit the HTML/JSON by hand or treat it as canon.

`agent-harness scenario-matrix` prints the executable release-gate catalog for topology-sensitive changes. Use it with `agent-harness invariants`, `agent-harness schema-registry`, and this contract when a diff touches channel identity/state, runtime queue/pipeline, Codex recovery, progress, final outbox/delivery, memory, or multi-agent ownership.

## Expected Vs Actual Gaps

Use this table for topology and knowledge-graph work whenever a design target is broader than current live evidence. Do not collapse "usable fallback" into "full parity" in docs, graph notes, memory summaries, or release handoffs.

| Area | Design Target | Current Evidence | Gap Label | Promotion Gate |
|---|---|---|---|---|
| Bounded receipt history and isolated maintenance owner | Interactive ingress, progress, runtime completion, and final delivery read source-authoritative bounded snapshots and only signal retention; expensive compaction runs under the independently supervised `ledger-maintenance-loop`. | Focused 100k history, busy-lock snapshot, wake-coalescing, failure-isolation, queue-index migration, and full-workspace staging tests are green. Candidate supervisor dry-run plans the owner; controlled live long-turn ordering/latency evidence remains pending. | `bounded-ledger-maintenance-live-gap` | Promote only after candidate preflight includes the dedicated owner and controlled live evidence proves progress/final delivery does not wait for retention, with bounded maintenance receipts and no recurring rebuild warning. |
| Task Scheduler plan/reconcile configured-loop parity | Regenerating the Windows Task Scheduler bundle preserves every enabled configured `supervisor.telegramLoops` runner while adding the isolated maintenance owner. | Fail-first/green plan regression and a staged nine-owner plan show the configured Telegram loop and `ledger-maintenance-loop` together. Live plan regeneration and fresh heartbeat readback remain pending. | `supervisor-plan-configured-loop-parity-live-gap` | Promote only after the guarded cutover regenerates the authoritative plan, all configured channel and maintenance owners are fresh, and reconcile reports no missing/stale/launch actions. |
| GPT-5.6 Max-only prompt/child/coordinator upgrade | GPT-5.6 routes accept catalog-supported efforts through exact `max`; exact `ultra` is unsupported and cannot be enabled through command, persisted state, child policy, or configuration. Prompt configuration is isolated per agent and exact full lane/backend generation. Heterogeneous child policies are immutable, child results carry bounded/redacted content or explicit failed omissions to the exact owner, and one exact-lane-safe master continuation owns user-facing output. | Fresh focused and full-workspace staging evidence is green for exact Max/Ultra command/catalog/runtime behavior, exact-lane prompt manifests, immutable heterogeneous child policy, typed mailbox/coordinator resume, and parent-only final routing. The 2026-07-15 guarded cutover and provider-visible T4 replay used the current exact V2 lane session, admitted two authoritative heterogeneous children (including exact `max`), completed the mailbox/resume path, and delivered one master final with no child final outbox. | `gpt56-max-prompt-coordinator-cutover-gap` | The initial cutover/T4 gate is passed. Promote to broader operational parity only after the scheduled read-only soak confirms no duplicate final surface, route/policy drift, queue convergence failure, or cross-lane regression; add a controlled cross-platform exercise when the changed deployment scope requires it. |
| Telegram final text delivery and terminal receipt precedence | Overlong Telegram final text should deliver as ordered provider-safe chunks under one logical delivery id, every provider message id should be captured as rendered-unit receipts, Telegram provider-permanent `message is too long` responses should terminal-skip instead of retrying unchanged payloads, and any terminal receipt must outrank later retryable failures for the same delivery id. | Staging tests cover UTF-16 chunk budgeting with labels, paragraph/line boundary preference, plain and rich-fallback chunk delivery, post-chunk permanent `message is too long` classification, Discord chunk regression, and `channel_delivery::tests::terminal_delivery_receipt_outranks_later_retryable_failed_receipt`. | `telegram-final-chunking-terminal-receipt-live-gap` | Promote after live or candidate-home provider evidence shows a long Telegram final produces ordered `(i/n)` messages, one delivery receipt with per-chunk rendered units, no retryable backlog, and no resurrection after terminal receipt. |
| Interrupted validation command observability | A same-lane newer turn that interrupts an in-flight Codex command should record `interrupted_by_new_turn`, command preview/cwd/log pointers, safe-rerun classification, and bounded continuation context; it must not be reported as a failed test without an observed failing exit code or assertion output. | Staging tests cover active command cancel evidence in `codex_runtime`, safe-rerun classification, runtime failure text that avoids failed-test wording, and structured `lastInterruption` prompt context with legacy fallback. | `interrupted-command-observability-live-gap` | Promote after controlled live-lane or candidate-home smoke proves interruption affects only the current execution lane, gateway/supervisor loops remain live, receipts contain bounded command evidence, and the continuation prompt names the resume action without stdout/stderr dumps. |
| openclaw-mem graph autonomous matching | openclaw-mem / openclaw-mem-engine graph recall, capture, routeAuto/autonomous matching, provenance, and freshness operate at full product parity through the active memory owner. | Graph topology extract is green for the harness support plane. The 2026-06-29 live cutover has a bounded subprocess bridge path for active `mem-engine` status/recall/store ownership with request/response mirrors, fallback telemetry, approved-store fail-closed behavior, env-or-harness-config bridge routing, and post-cutover proof that status/recall/read-path smoke run with `fallbackUsed=false`. Graph autonomous matching is still not parity. | `openclaw-mem-full-parity-gap` | Bridge/sidecar emits stable recall/store envelopes; Qdrant/native backend selected by the memory layer; routeAuto/autonomous matching fixture passes; provenance/freshness receipts are green; fallback is not used for the proof query; canonical writes remain governed by openclaw-mem policy. |
| Per-agent memory recall compartment | Non-main and public-facing agents should receive their own memory prompt/lifecycle/canvas/writeback context by default. Legacy/global imported memory should be opt-in, trust-tagged, and visibly scoped so a public or coach agent cannot accidentally inherit private `main` context. | Local Round10 gap-closure staging has `memory::tests::non_main_memory_prompt_context_excludes_global_imported_snapshot_by_default`, which proves a non-main agent recall context uses its per-agent memory root, reports `globalImportedSnapshotAllowed=false`, records source scope, and filters private global imported memory. The 2026-06-29 bridge-primary live cutover removes the active recall/store fallback dependency. The follow-up `memory::tests::public_agent_read_path_smoke_surfaces_source_allow_list_and_filtered_counts` broadens the candidate read-path smoke to expose public/non-main allow/deny source scopes, trust level, and filtered global-imported hit counts. | `per-agent-memory-recall-compartment-gap` | Before promotion, run the focused memory regression plus candidate-home status/read-path smoke that surfaces the same scope/filter telemetry. Do not claim openclaw-mem full graph parity while native/Qdrant backend proof, routeAuto/autonomous matching fixtures, or provenance/freshness receipts remain open under `openclaw-mem-full-parity-gap`. |
| Channel session history classification | Requests like "previous session" or cross-session search should resolve within the current platform/account/channel/user/agent lane unless a result is explicitly broad-scope project/global history. Agent-space history must not be treated as one blended pool. | Channel session history classification staging adds `memory_pack::tests::retrieval_scope_session_requires_same_agent_and_session_key`, `memory_pack::tests::retrieval_scope_session_rejects_bare_session_key_even_when_equal`, `memory_pack::tests::retrieval_scope_agent_private_rejects_bare_session_key_even_when_equal`, `memory_pack::tests::retrieval_candidate_does_not_fall_back_to_wrong_lane_concrete_history`, `memory_pack::tests::retrieval_candidate_uses_project_after_wrong_lane_concrete_candidate`, `memory_pack::tests::retrieve_wrong_lane_concrete_history_reports_scope_denied_not_missing`, `memory_pack::tests::retrieve_wrong_lane_concrete_then_project_returns_explicit_broad_scope`, and `memory_pack::tests::retrieve_wrong_lane_concrete_then_global_imported_returns_explicit_broad_scope`, proving concrete `session`/`agent-private` history is same-agent/session only, bare concrete keys fail closed, wrong-lane concrete candidates do not become arbitrary fallbacks, denial receipts stay visible, and explicit `project`/`global-imported` fallback still works. Account/channel/user axes are currently enforced through lane-qualified `sessionKey` rather than separate pack-artifact columns. | `channel-session-history-classification-gap` | Promote only after cross-session search/retrieval replay covers Telegram vs Discord and main vs non-main axes, receipts distinguish same-lane, cross-agent/session denied, and explicit project/global fallback, and any future store that has separate account/channel/user columns compares those axes directly instead of relying only on `sessionKey`. |
| Multi-agent platform isolation | Any configured agent can be added without rebuilding the pipeline, then own its workspace/prompt source, channel state, provider/model settings, memory namespace, context history, runtime lane, worker/subagent leases, and delivery receipts while sharing harness infrastructure. | Imported-agent registry, per-agent session paths, per-agent `/model` and `/think` overrides, provider-specific Codex homes, per-agent memory roots, agent-aware runtime dispatch, worker `master_agent_id`, and subagent lifecycle receipts are implemented. The Xiaoxiaoli group final-outbox regression showed one final-owner guard still used `main` as a proxy for parent ownership; Round17 changes that gate to lane-agent consistency, keeps owner-mismatch suppression, and adds `runtime_pipeline::tests::run_runtime_queue_once_writes_final_outbox_for_non_main_agent_owned_group_lane` plus `runtime_pipeline::tests::run_runtime_queue_once_suppresses_owner_mismatched_agent_final_outbox`. | `multi-agent-full-matrix-gap` | Add or maintain a reusable matrix that creates a new configured agent and proves independent workspace/prompt files, channel command state, provider/model override, memory/context history, worker lease/subagent lifecycle, final outbox, and delivery trace while sharing the same harness queues and loops. |
| Virtual session and working-context continuity | Long tasks should survive repeated compaction, repeated high-risk stream instability, or interrupted long-task slice overruns by rolling into fresh concrete sessions inside a stable virtual session, carrying a bounded working set so repeated compression, broken stream retries, or timed-out tool slices do not silently distort task state. `/new` is a task-boundary command and must not inherit the previous virtual/task memory context. | `context_rollover` records compact counters, virtual-session records, working-set memory, session indexes, rollover episodes, and guarded requeue receipts. The current implementation injects a bounded, pointer-only `Virtual session working context` prompt section from `resolve_virtual_session_working_context`, emits a missing-reply-metadata hint from same-lane queue anchors, closes prior virtual-session records on `/new`, backfills `codexThreadId` from Codex completion capture, separates `maxContinuationDepth` from compact-count thresholds, exposes stream-unstable continuation knobs, surfaces `lastInterruption` continuation guidance, and marks max-depth virtual sessions `terminal-failed`. Replay and catalog coverage includes `context_rollover::tests::three_turn_compact_rollover_replay_writes_continuation_working_set`, `runtime_pipeline::tests::stream_unstable_retry_continuation_requires_repeated_high_usage_stream_failure`, `runtime_pipeline::tests::polluted_thread_continuation_runs_at_terminal_failure_and_respects_depth_limit`, `runtime_pipeline::tests::tool_timeout_fallback_failure_retry_requeues_continuation`, `runtime_pipeline::tests::no_final_answer_terminal_interruption_requeues_continuation`, prompt missing-reply/root-boundary/interruption guidance coverage, `/new` closure coverage, Codex thread-backfill coverage, and the `virtual-session-rollover` S1-S5 scenario-matrix labels. Live validation has confirmed model-visible working-context prompt injection and a real `/new` task-boundary readback without carrying prior working-set context. Remaining evidence before promotion is broader live soak plus traced final-delivery continuity across real channel turns. | `virtual-session-continuity-gap` | Replay requirements are met for the current implementation. Promote only after live soak demonstrates model-visible continuity through real Telegram/Discord turns, traced final delivery remains correct, interrupted long-command slices continue from working-set evidence, and `/new` task-boundary behavior remains verified without cross-lane or cross-agent substitution. |
| Progress delivery and final-reply UX | A long-running channel turn should show bounded progress updates, then converge to one final reply or terminal notification without excessive provider edits, repeated body/status churn, non-terminal progress after completion, terminal progress closing before the source final provider delivery, or raw internal worker final text leaking into the visible progress action stream. The body/action lane is an event-history stream and may be capped; the status/current-step lane is a heartbeat/current-summary surface and must remain editable after the body cap. | A 2026-06-28 Telegram DM audit after 02:57 Taipei found one completed turn with one final outbox but more than 100 progress edit receipts, and a later in-flight turn with dozens of progress edits before any final outbox. A Discord long-running turn then showed the inverse UX failure: body/status counters reached `6` while internal events and typing indicators continued, making the visible panel look frozen. Round10 adds `response.progressDeliveryMaxNonterminalUpdatesPerLane` / `progressDeliveryMaxNonterminalBodyUpdatesPerQueue`, tracks delivered non-terminal lane counters in `agent-harness.progress-delivery-state.v1`, exposes `volume_limited`, keeps terminal convergence outside the volume cap, and adds status heartbeat after body cap via `progressDeliveryStatusHeartbeatAfterBodyCapMs`. `progress::tests::delivery_plan_status_heartbeat_after_body_cap_is_channel_agnostic` proves the same behavior for Telegram and Discord fixtures. Follow-up staging adds `progress::tests::delivery_plan_status_updates_immediately_for_new_current_step_after_body_cap`, proving a newly appended assistant execution summary bypasses the post-body-cap heartbeat interval and edits the status/current-step lane immediately. Round12 scenario-matrix follow-up adds `progress::tests::progress_surface_volume_replay_converges_without_post_terminal_churn`, covering Telegram and Discord in one replay: delivered body/status, capped body/action lane, immediate status/current-step update, heartbeat after cap, terminal convergence, and no late provider-visible churn after terminal delivery. Package D staging adds `progress::tests::action_stream_summarizes_internal_worker_results_instead_of_raw_final_text` and `progress::tests::action_stream_summarizes_structured_subagent_notifications_without_source_label`, proving subagent/worker result tool events, including structured subagent notifications without source labels, render as a bounded summary instead of raw worker final prose. Round17 final-order staging adds `progress::tests::terminal_progress_waits_until_source_final_delivery_is_delivered`, proving terminal progress waits when the same source queue has a final outbox row whose provider delivery receipt is not yet delivered. | `progress-delivery-volume-gap` | Promote only after a replay/scenario gate proves per-queue body/action edit caps, status/current-step heartbeat after body cap, immediate current-step updates for new assistant execution summaries, worker-result action summaries, repeated text-hash suppression, terminal progress convergence after source final provider delivery, final-outbox presence, and no post-terminal edit churn across Telegram and Discord. |
| Progress narration and final surface separation | When `assistantNarrationMode=progress_panel`, progress/narration deltas should be visible only through progress panels or audit receipts. Final `agent-reply` outbox payloads should contain the final answer or terminal notification, not the in-progress diagnostic stream. | A 2026-06-28 local Telegram DM probe found a prior long progress stream written to channel outbox as `agent-reply` even though the completion receipt recorded `assistantNarrationMode=progress_panel`, `assistantFinalChars=140`, and `assistantNarrationItems=0`. Round10 adds runtime-pipeline replay gates for already-recorded completion repair proving progress-panel narration stays out of repaired final outbox payloads for both Telegram and Discord. A later Telegram DM imagegen turn exposed the stdout-recovery subcase: a recovered completed turn had no `final_answer`, only progress-panel narration, and wrote that narration as final `agent-reply`; local follow-up staging adds `codex_runtime::tests::run_codex_runtime_rejects_stdout_recovery_narration_without_final_answer`, which fails closed instead of using narration/raw assistant text as final. | `progress-final-surface-gap` | Promote only after the replay/scenario pack also covers compact-before-turn fallback or fresh-thread recovery and asserts Telegram and Discord final outbox payloads contain only the final answer while progress text stays in progress receipts/panels. |
| Codex tool-use timeout recovery | Long external tools should not be allowed to silently stall the app-server until the parent queue item is dead-lettered. The harness should stop the stalled tool path, surface the timeout to the model, and allow bounded recovery, virtual-session continuation, or a clear blocker report. | Round10 adds active tool-use tracking for Codex item events and converts an in-flight tool idle timeout into one fresh-thread recovery prompt with `toolUseTimeout` receipt metadata. Round17 interrupted-task staging rotates stale timeout stdout before retry recovery, routes failed tool-timeout fallback to virtual-session continuation, carries bounded tool summary into the working-set decision/requeue reason, and keeps provider-outage-shaped timeouts out of the continuation predicate. This remains short of a dedicated per-tool supervisor with resumable cancellation. | `tool-use-timeout-recovery-gap` | Promote to full parity only after per-tool cancellation semantics, retry budgets, trace replay, and long-review soak tests prove a timed-out tool can be killed or isolated without losing queue trace, final outbox, continuation prompt evidence, or follow-up model control. |
| Codex absolute deadline drain and productive continuation | A fixed interactive wall-clock deadline must not replay a turn that already changed files, ran tools, or emitted substantive narration, and it must not invite new long work or same-turn steering in its final minutes. | Staging classifies productive absolute timeouts from concrete Codex stdout item evidence rather than raw event volume, resolves sibling eligibility through effective queue terminal state, sends one receipted deadline-drain `turn/steer` in the final 10% capped at three minutes, and defers late user steering to retained next-turn/passive context. Focused replays cover productive versus handshake-only timeout, historical-terminal versus genuinely pending sibling, one-shot drain steering, and deadline-window user-steer deferral. | `codex-absolute-deadline-drain-gap` | Promote only after a candidate or controlled long-turn replay shows no new long command after the drain receipt, one guarded continuation for productive timeout, no continuation for handshake-only timeout, no original prepared-turn replay, and retained late steering visible on the next exact-lane prompt. |
| Codex app-server startup and executable stability | Service-mode runtime should invoke a spawn-stable Codex executable and classify startup/write failures as terminal receipts instead of losing queue trace or wedging channel callbacks. | A 2026-06-29 live incident found runtime execution vulnerable to Windows command-shim/native-binary drift after a Codex app update: app-server startup could exit with `0xC0000142`, and stale Discord queues needed replay or explicit terminalization. Source hardening now prefers native Windows vendor `codex.exe` for npm `codex.cmd` shim installations and uses the shim only as fallback, while app-server startup/write failure records a terminal protocol-error receipt with focused regression coverage. The same follow-up makes CLI default Node resolution prefer an explicit Windows `node.exe` when no `--node-exe` is supplied, and the live Discord scheduled gateway script was hotpatched away from the bare `node` PATH shim. The live runtime was hotpatched to an explicit native Codex executable; broader binary cutover and soak remain separate gates. | `codex-startup-executable-stability-gap` | Promote only after Windows npm-shim/native-vendor tests, MSIX/extensionless rejection tests, Node default-resolution tests, early-exit/write-failure terminal receipt tests, and a live post-restart smoke prove pending queues and `runtimeOpenItems` converge to zero across Discord and Telegram. |
| Runtime queue terminal-control authority | Operator-written queue controls must be authoritative before runtime selection, preparation, lease return, run-once execution, and progress delivery. Terminal run-once receipts, queue-skip control receipts, scoped-stop markers, and quarantine markers must prevent runnable work without manual state surgery and must close visible progress with one final terminal surface before going silent. | Slice A adds a resolver-backed terminal-control check in runtime selection/preparation and stale lease reconciliation, makes historical terminal run-once statuses sticky instead of latest-status-wins, writes at most one `suppressed` run-once receipt with terminal-control metadata, terminalizes repeated unresumable prepared executions with a quarantine marker, and keeps retries on fresh queue ids. Round16 Slice B extends suppressed runtime early-return paths to write terminal progress evidence, with scoped-stop/no-prepared and quarantine regressions proving one final progress surface followed by silence even after late progress events. The checked-in `runtime_pipeline::tests::e2e_1_ghost_queue_replay_from_sanitized_fixture` covers the public-safe ghost-queue replay surface; live/candidate-home proof remains a promotion gate. | `runtime-terminal-control-gap` | Promote after T1-T6 regression coverage plus checked-in E2E-1 replay and live or candidate-home replay prove terminal-controlled pending queues do not resurrect, suppression receipts are idempotent, progress emits one final terminal surface and then no further status edits, retry uses a fresh runnable id, and `no-prepared-execution` churn stops without manual queue edits. |
| Deterministic cron freshness and catch-up | Cron canon freshness must be evaluated by status/health independently of the keeper job, deterministic cron executions must be answerable by canonical cron id, and restart catch-up behavior must be explicit and receipted per job class. Each occurrence must come from one exact crontab source, use the declared timezone/calendar fields, and apply bounded per-entry timeout/attempt policy before enqueue. Notification/sender and backup-class jobs must not execute stale work unless an operator forces that path and the receipt says so. | The scheduler now filters exact crontab filenames, validates canonical `timeoutMs` / `maxAttempts`, honors `TZ` / `CRON_TZ` for current and catch-up slots, supports calendar day/month fields, passes stable occurrence metadata to workers, and terminalizes exhausted jobs before lease. Incident-shaped replay covers attempt-cap bypass, process-tree timeout, invalid policy, source-copy filtering, timezone, and unsafe backup catch-up suppression in addition to existing health/status and E2E-5 evidence. | `deterministic-cron-freshness-gap` | Promote after focused incident replay plus a candidate/live dry-run prove zero unsafe stale backup enqueue, one intended source per entry, valid execution bounds, correct local schedule evaluation, and post-cutover worker/scheduler readback shows no retry growth or duplicate occurrence. |
| External review loop resilience | External review/second-brain output is progress evidence for the parent task unless the parent queue explicitly reaches final completion. A Claude review result, timeout, auth failure, read-only inspection result, or non-owner agent result must not be mistaken for workflow closure. | Local Round10 gap closure adds `codex_runtime::tests::run_codex_runtime_keeps_external_review_recovery_as_evidence_not_final` and `runtime_pipeline::tests::run_runtime_queue_once_keeps_external_review_evidence_resumable_without_final_outbox`, proving review-only recovery output is evidence, the parent queue remains resumable, and no final outbox is written. Round14/Round17 staging adds `runtime_pipeline::tests::run_runtime_queue_once_suppresses_read_only_review_final_for_implementation_goal` and `runtime_pipeline::tests::run_runtime_queue_once_suppresses_owner_mismatched_agent_final_outbox`, proving implementation-goal read-only review text and owner-mismatched completed output stay off user-facing `agent-reply` rows while a routed non-main channel parent remains eligible to write its own final. | `external-review-loop-resilience-gap` | Keep the runtime-pipeline replay green and add live/soak evidence before promotion: final outbox closure must only happen after the parent workflow completes or emits an explicit terminal blocker, and structured owner/session identity must match before a completion can become a visible final reply. |
| Context preflight high-usage guard | Bound Codex threads with very high recorded usage or known thread-health pollution should compact, checkpoint, or rotate to a fresh thread before heavy image/external-tool continuations, even when prompt bytes are modest and no `modelContextWindow` ratio is configured. | Local Round10 gap closure adds `codex_runtime::tests::run_codex_runtime_preflight_compacts_high_usage_bound_thread_without_model_window`, which failed first for a 136602-token replay with `compactBeforeTurn=false` and now passes by forcing compact through `highContextUsageCompactTokenLimit`. Round14 backlog staging adds `codex_runtime::tests::context_preflight_rolls_over_bound_thread_inline_image_bloat` and `codex_runtime::tests::retryable_protocol_error_after_bloated_thread_rolls_over_to_fresh_thread`, proving inline-image/oversized-output polluted bound threads use fresh-thread rollover before another doomed turn/start instead of resuming or compacting the same polluted thread. | `context-preflight-high-usage-gap` | Promote only after Telegram/Discord replay proves high prior bound-thread usage and polluted thread-health receipts trigger compact/checkpoint/fresh-thread before heavy continuations, fallback does not reuse the overloaded context, and final outbox/progress surfaces remain separated. |
| First-class command plane | User commands should be parsed as control-plane events before model dispatch, mutate scoped channel/session state deterministically, emit machine-readable command receipts, and skip LLM execution when the command is fully handled by the runtime. Commands should not depend on a chat-facing agent noticing and obeying command-shaped text. | Agent Harness has a command path for `/new`, `/think`, `/model`, `/stop`, `/status`, `/steer`, `/btw`, and protected restart requests: `channel_runtime` builds `CommandReply` steps, `channel_state` writes scoped state plus command receipts, and command replies do not enqueue agent turns. Round16 Slice B hardens command parsing/receipt uniformity and adds a checked-in `agent-harness-cli::tests::e2e_4_restart_gateway_staging_closed_loop_replay` for the gateway restart request -> consumption -> heartbeat -> completion ack chain. Remaining live proof is provider/supervisor smoke: a real supervised child restart and Discord delivery evidence, plus any lifecycle commands still intentionally model-assisted. | `first-class-command-plane-gap` | Keep central command registry coverage for command-only, command-plus-reply, and command-plus-enqueue cases; emit stable command receipts for applied, unsupported, unknown, and skipped-not-command outcomes; prove handled commands do not create model turns; prove same platform/channel/user but different `agentId` state isolation; prove `/stop` cancellation is immediate and source-correlated; document which lifecycle commands are runtime-owned versus model-assisted; before live promotion, prove the E2E-4 restart loop with a real supervised child and provider-side ack delivery. |
| Skill ecosystem closed loop | Skills should behave as runtime state with deterministic per-agent discovery/selection, guarded prompt exposure, proposal-mediated mutation, first-class autonomous review/apply, reversible lifecycle curation, checksum-locked packs, and aggregate health evidence. | Staging adds matcher v4 with mixed CJK tokenizer, category/tag scoring, exact-agent usage priors, per-agent source roots, hard `agents` allowlists, lifecycle and explicit/model invocation gates, catalog retrieval, `skill-view`, `skill-lint`, `skill-guard`, reversible curator archive/restore/pin, file-based skill packs, `skill-synthesize` with autonomous apply by default, `skill-apply --autonomous`, `skill-doctor`, schema entries, and invariants I8/I20. `lifecycle: retired` / `retired_historical` and a non-matching `agents` allowlist cannot reach prompt selection; disabled model/user invocation remains independently enforceable. Direct writes remain forbidden; autonomous apply still uses proposal ids, checksum validation, target locks, backups, lint/guard decisions, and receipts. | `skill-ecosystem-closed-loop-gap` | Promote only after focused tests plus the staging closed-loop replay prove CJK selection, retired/disabled/agent frontmatter gates, a main/non-main/third-agent source and usage-prior isolation matrix, no-skill complex turn synthesis, autonomous review/apply, re-selection of the created skill, pack export/import/remove round-trip, curator dry-run, `skill-doctor --json`, public/private hygiene, and no live cutover until explicit operator approval. |
| Provider request acceleration command | Users should be able to toggle provider-native fast/priority request behavior for the current agent/session without mutating prompt text, leaking across agents/channels, or sending unsupported request parameters to Codex/proxy/provider routes. | Hermes Agent upstream implements `/fast normal`, `/fast fast`, and `/fast status` as a command-state/request-override path: command registry plus CLI/gateway handlers, model capability checks, per-turn `request_overrides`, OpenAI `service_tier="priority"` for supported routes, Anthropic `speed="fast"` only for supported native routes, and defensive stripping for incompatible routes. Agent Harness now handles `/fast status|fast|normal|on|off` as command replies, persists session/per-agent fast-mode state, writes provider request policy into turn/runtime plans, gates Codex app-server acceleration by the local Codex model catalog `serviceTiers`, sends `serviceTier=priority` for supported OpenAI/Codex app-server models, and sends `serviceTier=default` to reset supported routes when mode returns to normal. Unsupported models, OpenRouter/proxy routes, and non-Codex native routes remain fail-closed with no `serviceTier`/`speed` field. | `provider-fast-request-override-gap` | Remaining gate is provider breadth and live replay: keep Discord/TG and main/non-main isolation smokes, prove provider/model switches recompute or clear stale overrides, replay live `/fast` on a Fast-capable Codex app-server model and an unsupported route, and add separate provider-native implementation before sending Anthropic `speed` or proxy-specific acceleration fields. |
| Channel media delivery | Outbound files should be delivered through channel-native media APIs only after harness policy accepts the local path or resolvable artifactRef, while inbound and referenced channel media should remain prompt-safe artifacts with provenance. | Channel-media staging adds a shared media extension/kind policy, protected-span MEDIA directive parsing, rejection/lint receipts, inbound-media and generated-image/generated-media artifactRef resolution into attachments, Telegram photo albums plus document/audio/voice/video sends with caption overflow handling, Discord multipart file batching, Telegram non-image document/voice/audio/video/static-sticker/reply-media artifact extraction, Discord referenced-media fetching, and native-image-input context-bloat preflight. ArtifactRef resolution is intentionally limited to harness-owned URI namespaces plus the same outbound policy gate; arbitrary provider URLs and raw paths remain refused. | `channel-media-delivery-gap` | Promote only after parser/policy/lint gates, provider batching gates, inbound/referenced media hygiene gates, native-image bloat rollover, public schema/topology docs, and live Telegram/Discord smoke all pass. Keep `media.nativeImageInput` and `media.lintFailClosed` default-off until post-cutover enablement evidence exists. |
| Rich message presentation | Final replies, attachments, links, buttons/actions, captions, and provider-native rich UI should be modeled as a semantic presentation payload rendered by channel adapters, not as raw model-authored Telegram/Discord syntax. | Agent Harness already has durable `ChannelOutboundMessage` text/attachment outbox, Telegram HTML-capable final sends through scoped formatting config, Discord 2000-character chunking with `allowed_mentions.parse=[]`, provider reply/thread intent, attachment delivery, and progress/final separation. Package A-D staging adds optional `agent-harness.rich-message-presentation.v1`, fail-closed validation, old-outbox compatibility, Telegram/Discord render fixtures, rendered text/media/action units, per-unit delivery receipt fields, partial retry semantics, provider send helpers, and ordinary final-to-presentation conversion. Round14 backlog staging adds `rich_presentation::tests::plain_final_bridge_renders_markdown_subset_as_semantic_blocks`, `channel_delivery::tests::rich_delivery_receipt_records_units_and_retries_partial_failure`, `channel_delivery::tests::delivery_receipt_without_presentation_field_stays_readable`, the Telegram closed-loop delivery receipt test, and Telegram/Discord rich sender fallback tests, proving plain-final Markdown subset parsing, Telegram HTML provider payload rendering, provider render-mode capture, fallback reason telemetry, and full-text-preserved fallback receipts for both channel adapters. Remaining gaps are live cutover/soak, Telegram inline action sends, Discord component sends, live preview integration, artifact/provider URL redaction coverage for generated presentation payloads, and clicked-action ingress re-entry evidence. | `rich-message-presentation-gap` | Promote only after an optional `agent-harness.rich-message-presentation.v1` schema exists, Telegram and Discord render fixtures prove escaping/chunking/no-ping behavior, ordinary final replies emit presentation payloads without progress/narration, provider receipts capture rendered units plus render/fallback/full-text status, multi-unit rich batches record per-unit provider receipts, partial delivery cannot be marked complete, media captions do not duplicate text/media sends, raw artifacts/provider URLs/file IDs are excluded, and Telegram/Discord action interactions re-enter through normal channel identity, `agentId`, session, and permission gates. |
| Generic artifact/context hygiene | Binary and bulky artifacts should be stored and referenced through harness artifact metadata after extraction. Durable main-session prompt, memory, continuity, progress previews, and final replies should receive bounded extracted facts plus artifact refs, not raw bytes/base64/provider URLs/document bodies/tool blobs. | Round10 now extends `agent-harness.inbound-media-artifact.v1` with optional `lifecycleStatus` and `extractionSummary`, adds generic prompt redaction for raw payload signatures/provider URLs/tokens/cookies, and covers Hermes-style classes with `media::tests::artifact_prompt_hygiene_renders_generic_bounded_summaries_not_payloads` plus runtime queue prompt coverage. Follow-up staging adds `codex_runtime::tests::codex_progress_redacts_artifact_payloads_from_tool_previews`, proving generated image/tool output previews do not persist raw `data:image`, base64, provider URLs, or token-bearing strings in progress events. Discord attachment extraction follow-up adds `agent-harness-cli::tests::discord_gateway_text_attachment_becomes_bounded_artifact_metadata`, `agent-harness-cli::tests::discord_gateway_image_attachment_is_artifact_reference_without_payload`, `agent-harness-cli::tests::discord_gateway_rejects_non_discord_attachment_url_without_fetching`, `agent-harness-cli::tests::discord_gateway_attachment_download_failure_redacts_provider_url`, `agent-harness-cli::tests::discord_gateway_image_attachment_requires_supported_image_bytes`, `agent-harness-cli::tests::discord_gateway_json_like_attachment_uses_json_artifact_extension`, and `media::tests::discord_text_artifact_uri_resolves_and_passes_generic_safety`, proving Discord text/image attachments are copied into artifact storage and prompt-rendered through artifact refs plus bounded text summaries without raw CDN URLs/tokens, while non-Discord URLs, raw provider-error strings, fake image bodies, and JSON-like extension gaps fail closed. This is prompt/progress/ingress-surface hygiene, not a complete artifact lifecycle service. | `generic-artifact-context-hygiene-gap` | Promote only after an end-to-end artifact-heavy Telegram/Discord replay proves extraction writes bounded facts, raw artifacts remain retrievable by pointer, memory/continuity do not persist blobs, generated media/tool outputs are stored as artifacts/receipts with capped previews, and image-only compact is not used as proof for audio/browser/document/log classes. |
| Codex image route-vs-input classification | Image generation receipts should distinguish a broken global image route from a specific prompt/reference input that failed to call the image tool after the route has been proven available. | A Round9-4 white-lounge raw-final recheck showed known-good inputs and canary route probes succeeded through the same gateway, while the target input returned `IMAGE_GENERATION_FAILED_NO_TOOL`; older receipts classified that as `image_tool_unavailable`. Round10 makes the gateway run a route probe before returning unavailable and emits `image_tool_not_called_for_input` when the canary proves the route is available. | `image-route-vs-input-classification-gap` | Keep fake-route tests plus at least one staged route canary receipt proving available route and input-specific failure classification before using the classification for production image-task fallback decisions. |
| Repo code topology graph | A queryable repo/code knowledge graph can answer architecture, dependency, and diff-impact questions for behavior-changing changes. | Topology contract exists as canonical text; openclaw-mem topology extract currently covers cron/script/repo support-plane nodes, not full Rust code relationships. | `repo-code-graph-support-gap` | Understand-Anything or equivalent graph build runs with a reviewed ignore policy, public hygiene boundaries, commit hash metadata, and reviewer-confirmed usefulness against the topology contract. |
| Supervisor service health precedence | `healthz`, `status --json`, and `supervisor-reconcile` should present one canonical managed-service health view or explicitly label when service registry metadata is non-authoritative behind a fresher loop heartbeat. | A 2026-06-28 Round10 post-cutover readback showed `healthz ready=true live=true`, fresh `discord-gateway-loop` heartbeat, and `supervisor-reconcile --all --dry-run` with no missing/stale services, while `healthz.supervisorServices` still exposed an older `discord-gateway-loop` service record as `status=spawning` / `stale=true`. Local follow-up staging adds `status::tests::loop_status_prefers_fresh_loop_heartbeat_over_spawning_service_state` and `health::tests::healthz_prefers_fresh_loop_heartbeat_over_spawning_service_state`, aligning `status`/`healthz` read models with reconcile precedence for a fresh alive Discord gateway heartbeat. | `supervisor-service-health-precedence-gap` | Promote only after service-state, loop-heartbeat, `healthz`, `status --json`, and reconcile output use the same precedence rules or surface stale service metadata as historical/non-authoritative, with a Discord gateway spawn-boundary scenario proving no false stale alarm when the Node heartbeat is fresh. |
| Release-gate scenario replay | Every topology-sensitive change maps changed modules to required scenario packs and invariant replay. | Invariants and release checklist exist; agent-boundary regression added for session freshness. `agent-harness scenario-matrix` now exposes a reusable catalog covering agent-boundary, final-outbox/delivery/trace, virtual-session rollover, progress surface/volume, and multi-agent memory compartment gates. | `scenario-matrix-coverage-gap` | Keep the scenario matrix catalog green and use it as the release gate selector for changed components. Promotion beyond coverage scaffolding still requires each selected scenario pack to have runnable replay evidence or a documented stronger equivalent. |

### Round16-1 Progress Ordering Binding

- `progress-delivery-volume-gap`: progress delivery preemption is queue-local. A global wake from another lane must not drop the whole pending batch, first-surface sends are not preempted before any provider message exists, and same-queue providerless orphan surface claims are reclaimed without waiting for the claim TTL.
- `runtime-terminal-control-gap`: progress delivery must consume durable terminal-control evidence before rendering non-terminal cached progress. Terminal-controlled queues may emit one terminal close edit for existing provider surfaces, record `progressSuppressedReason=terminal-control-present`, drop cached ghost events only after the close receipt is recorded, and then stay silent. Normal terminal progress paths keep their compacted terminal context so late events and retryable lane failures do not suppress legitimate retries.

### Battle-Set Progress Source-Authority Binding

- A last-committed or unavailable progress index snapshot is diagnostic evidence only. It must not create, edit, or replay provider-visible progress from `compactedEvents`; the bounded cache is retained until a fresh source projection can rebuild the plan.
- A fresh projection may contain retained history after a reset or migration. If a queue has no existing provider surface and its latest event is older than the progress freshness window, it is suppressed rather than creating a late `Send`; an existing provider surface may still converge through its normal edit/terminal path.
- `progress-delivery-loop` owns a watched stop file. It must check before provider I/O and release every unattempted fresh-send claim when stopping, so restart/cutover cannot strand a short-lived claim or require a broad process kill.

### Battle-Set Controlled Coordinator Replay Binding

- A provider-visible coordinator replay is an operator-only verification surface, not a general subagent import or recovery command. It must require an explicit bounded source marker, exact full-lane identity for `main`, its currently active exact channel session when v2 state exists, authoritative catalog admission for that lane and every planned child execution agent, exactly two distinct child models with at least one exact `max`, and a separate explicit confirmation.
- The command must use the existing V5 transactional child/wait/watchdog path. It must not create child work through direct database edits, generic historical-ledger selection, or an untyped runtime queue append.
- Child turns remain worker-origin internal work. The only provider-visible final is the exact master continuation after the mailbox and durable coordinator contract completes; failure evidence is retained privately and never retried by a scheduled self-check.

## Documentation Update Rules

- If a behavior change alters a component row above, update this file in the same change.
- If the public capability or operator command surface changes, update `README.md`, the relevant public reference, and any operator-only runbook maintained outside the public surface.
- If a release gate changes, update `agent_harness_core::quality::release_checklist` and the operator-only release checklist when one is maintained.
- If an invariant changes, update both `docs/invariants.md` and `agent_harness_core::quality::invariant_catalog`.
- If a channel smoke expectation changes, update the operator-only channel self-check without adding private evidence to this document.
- If this file or the current live-validation summary changes in a way that affects human topology review, run `python tools\generate_topology_explorer.py` and review `docs/topology-explorer.html`.
- If the change creates local-only evidence, keep it under `.debug/` or `.agent-harness/state/` and summarize it without raw private IDs in tracked docs.
- For the v0.8 state/schema boundary, document migration and rollback asymmetry explicitly: the new binary must read supported legacy artifacts, but the old binary must not be assumed to read newly written V2 artifacts. A rollback plan must either restore the matching pre-cutover state backup or quarantine/convert V2 state before starting the old binary.
